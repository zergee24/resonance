# AI 歌单产品规格

规格版本：0.3.0
产品形态：本地 macOS 应用
实现目标：用真实频响、真实音频和用户选择的喜欢的声音参考，得到逐频段、逐时间的接近度与可试听比较结果。

## 1. 产品目标

共鸣用耳机频响和歌曲的真实 PCM 频谱做本地计算，帮助用户按歌曲查看耳机表现，或按一副耳机寻找值得试听的歌曲。

名称、歌手、流派和候选歌名可以帮助整理资料，但不进入声学计算。数值结果描述参考偏差或谱形变化，不等于“音质分”，也不代替用户试听。

首版只处理实际得到的音频和曲线。没有真实频谱，或没有选定参考曲线时，不显示数值排名；不会用歌名、标签或空结果补成推荐。

## 2. 第一次可用路径

1. 打开“我的声学资料库”，点击“使用拉斐尔示例”。应用加载 HBB 的真实 `Artipical Raphael` 右声道曲线：480 个点，19.5 Hz–20 kHz。
2. 启动时幂等加载 HE1、Alter Ego 无点档位两条毁 HiFi 真实参考，各 957 点、20–19896.97461 Hz；默认纳入偏好集合，用户明确关闭后保持关闭。它们不是已拥有耳机。拉斐尔样例同时保留平直计算基线。
3. 导入一首本地音频，或采集播放器正在播放的一段真实音频。
4. 在“根据歌曲找耳机”中选歌和拉斐尔，查看偏好接近度与 D；也可以在“根据耳机找歌曲”中选拉斐尔和歌曲集合。
5. 结果页显示实际共同频段、频段明细和说明。只要缺少音频或参考，相关数值留空并说明原因。

这条路径是产品的最小验收闭环。其它网页读取、歌单整理和自动采集都不能替代真实音频加曲线的计算结果。

## 3. 两种工作方式

### 根据歌曲找耳机

固定一首已导入或已采集的歌，在“我拥有”的耳机中按个人参考接近度排序，并查看 D、Dhigh 或 C。跟随当前播放时，有精确歌曲 ID 且存在本地分析则先显示已有录音结果，本次录音仍持续积累。可以只有一副耳机；这时仍展示该耳机的实际结果和频段证据，不制造相对优势。

### 根据耳机找歌曲

固定一副耳机和候选歌曲集合，默认按偏好接近度排序；也可选谱形变化 C、参考偏差 D，或 10–20 kHz 偏差 Dhigh。多参考分别评估整段，取整体偏差最低的完整参考结果作为排序值，同时显示其余参考结果。不平均、逐频或逐帧拼接参考，不声称为喜欢概率。

候选歌曲可以来自本地音频，也可以来自读取到的已有网易云歌单。尚未有真实频谱的歌曲只显示待采集或资料状态，不进入数值排序。

## 4. 曲线、参考和测量信息

- 耳机曲线只接受实际频率响应数值；频率轴、dB 值、声道、来源和有效范围随资料保存。
- 参考曲线与耳机曲线一样可以从 CSV/TXT 或页面数值导入。平直参考是内置计算基线，用户可以换成任意已有参考。
- 测量设备、耦合器、耳套、补偿方式和 EQ 状态作为备注保存，供用户理解结果；不会仅因备注为空或文字不相等就阻断数值。
- 计算只在音频频谱、耳机曲线和参考曲线的实际共同频段内进行，不把曲线外推到没有数据的频率。
- 参考缺失、耳机曲线缺失、没有真实频谱或共同频段不足时，不产生 D、Dhigh、C 数值，并给出简短原因。
- 任何参考都只是计算坐标。参考曲线本身没有被证明代表“好听”，跨设备或跨测量体系的含义需要结合备注和实际试听判断。

页面读取只使用应用内正常可见的 HTTP/HTTPS 内容，区分频响、失真、相位和阻抗图表。页面失败、登录受限或数据不足时保留真实错误，不把图片或图表标题当成频响点。

## 5. 音频输入与采集

本地文件导入标记为完整文件，并保存原始采样率、声道和格式信息。文件分析依赖文件实际内容，不把元数据时长当成频谱证据。

播放器采集保留收到的原始格式、采样率、声道、实际录音秒数、丢帧和连续性信息。连续录音没有 30 秒上限，直到用户停止、暂停、切歌、关闭或退出；应用关闭和退出前会停止当前录音并保存已有内容。

每次采集都报告实际录音时长。无法证明完整覆盖时显示完整度未知或片段，不补造缺失的开头、结尾或跳过区间；不使用界面轮询容差认定整曲，也不人为截尾。

片段、小缺口或身份只有候选标题时，仍可基于已采内容给出估计，旁边写明“按已采内容估计”及简短限制。候选歌名不会伪装成准确平台 ID；用户可手动选择歌单中的对应歌曲绑定录音。

播放 EQ、响度、空间音效等状态读不到时记录“未知”，不把未知直接抹掉结果。结果说明应提示它反映当前播放器输出，不能假装是未处理母带。

“自动积累听歌资料”首次开启；用户手动关闭后记住选择，下次启动保持关闭。开启后只跟随实际播放，不主动切歌或批量播放。权限、进程退出、启动超时、格式变化和无有效音频都显示实际错误，允许用户手动处理或重试。

macOS 15+ 优先使用系统 Now Playing 读取歌名、艺人、专辑、进度、播放状态和来源应用（Bundle ID / PID）。来源随本地录音保存。系统元数据不可用时回退辅助功能与用户开启的窗口 OCR；macOS 14.2 保留这两种备选。

系统播放项的不透明标识只用于区分播放段，不当作网易云歌曲 ID。自动采集仅针对网易云，系统来源与目标进程不符时停止旧段并只显示元数据；其他应用不会被错误绑定到网易云录音。歌曲信息仍不参与声学评分。

## 6. DSP 与指标

分析器保留原采样率和声道，使用 Hann 窗 STFT 生成逐帧 PSD。频带能量按线性功率与实际频率单元宽度积分，不能直接平均 dB，也不默认降采样或混成单声道。

个人参考模型在原始 FFT 网格上做功率积分：20 Hz–10 kHz 使用 Glasberg–Moore ERB 频带几何，10–20 kHz 用较宽工程频带。每帧、每声道先算带内原始与响应变换后功率，再求 dB 差。谱带和时间能量采用固定指数 0.3 的压缩权重；它是减轻强频段/响段支配的工程选择，未冒充 ISO 响度。一个参考只拟合一个跨全段、跨声道的全局电平偏移。输出整体 RMS、10–20 kHz 独立归一偏差、按压缩帧能量加权的 P90、录音内最大偏差时间和带内证据。相同输入电平或曲线整体电平变化不应改变距离。

所有选中参考使用共同实际范围；20 kHz 以上单列明细，其能量不影响主排名的归一化。部分录音仍给估计，纯静音不生成 0 dB 最佳结果。PEQ 展示分段与听觉频带汇总分开。没有个人参考时保留已选单参考路径。原始 D/C 指标保留如下：

令 `H(f)` 为耳机曲线，`R(f)` 为参考曲线，`Δ(f)=H(f)-R(f)`。对歌曲在共同频段的能量 `E(f,t)` 加权，并允许一个全局电平偏移 `g`，保留原有参考偏差计算：

`D = sqrt(Σ E(f,t) × (Δ(f) − g)² / Σ E(f,t))`

D 越小表示在选定参考和实际共同频段内越接近；它不是响度、失真或主观好听程度。C 以左右声道的歌曲能量分布和耳机响应变化描述谱形改变；C 越大只说明改变更明显。

Dhigh 使用同一计算在 10–20 kHz 的实际共同数据上重新加权。有有限且非零的高频能量就给估计，低能量只作说明；只覆盖其中一部分时标明实际高频范围。没有高频数据或能量时数值留空。20 kHz 以上单独展示实际频段证据，不外推。

频段明细显示歌曲能量、耳机相对变化、偏差贡献和实际覆盖状态。频段相对变化与 D 使用同一全局电平对齐，避免把原始测量绝对电平当成增益。两个模式都按参考和实际计算范围明确分组。无音乐内容、部分覆盖、测量范围之外和分辨率不足分别说明。

展示采用便于 PEQ 观察的约 1/3 倍频程分段：边界从 20、25、31.5、40 Hz 延续到 10、12.5、16、20 kHz；扩展频段另列 20–25、25–31.5、31.5–40 kHz 等，最高至实际共同带宽。计算始终使用实际 FFT 频率网格和 log-Hz 曲线插值，展示分段不代替计算分辨率，也不宣称是唯一声学分类。

## 7. 歌单与导出

应用可以在内置网页中读取用户已有网易云歌单的可见歌曲 ID、标题、艺人和原始顺序，并标记当前读取是否完整。读取结果用于选择候选歌曲和手动把真实录音绑定到歌单原始行。

用户预览名称、歌曲及顺序后，应用通过已登录的正常网易云网页创建新歌单并逐首添加，再回读核对歌曲和顺序。已分析片段可以加入；缺平台 ID 的录音需先手动绑定。原歌单保持不变，本地 JSON 导出并行保留。

创建提交前保存一次轻量待核对记录；未知结果不会自动重放创建。用户可查看网易云、填写已创建的目标链接重新核对，或人工确认后开始新的创建。预览在本次窗口内固定，每次重新准备从当前结果生成。

## 8. 结果边界与范围

高频结果受音频采样率、播放器处理、测量带宽和真实内容影响；高频有数据只说明“有可计算证据”，不说明一定更好听。佩戴、耳套、个体耳道和主观偏好也可能改变试听感受。

已定位并修复空闲播放器首次出音导致的启动等待；生产 AudioCapture 到 CAF 的持续录音复核、网易云识别和整曲采集仍以 `TESTING.md` 为准。规格条目本身不等于通过验收。

本轮不包含自动 EQ、播放器替代、云端音频、全库爬取、语义推荐、批量自动播放和其它操作系统支持。

## 9. 可追溯性

每条结果关联实际音频、耳机曲线、参考曲线和分析参数，来源与备注可在资料库回看。

验证报告记录代码版本和实际观察；文档中的目标行为不替代真实构建、脚本和业务验收。

研究依据与工程取舍：[听觉与偏好研究](docs/research/headphone-preference.md)、[超高频研究](docs/research/extended-high-frequency.md)。研究中的总体偏好不直接等于本工具的个体歌单排序；实际听音对应关系需继续由用户试听检验。
