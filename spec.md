# AI 歌单工具 Spec

版本：0.3（讨论稿，可作为实现与验证依据）

日期：2026-09-24

产品形态：本地 macOS 应用

规格阶段交付：本规格文档。2026-09-24 用户已授权首版实现与本机测试；当前实现范围、验证证据和未完成项见 README.md 与 TESTING.md，不能将规格条目自动视为已经交付。

## 1. 产品目标与已确认的 Intent

基于耳机实测频响与歌曲真实音频的时频特征，计算耳机对具体录音的频段呈现，帮助用户发现值得用某副耳机欣赏的歌曲，并在用户拥有的耳机中，为当前歌曲推荐更适配的一副。

产品只有两个主模式：**根据歌曲找耳机**、**根据耳机找歌曲**。前者固定歌曲、比较耳机；后者固定耳机、筛选歌曲。读取当前播放歌曲是前者的输入方式，从已有歌单生成子歌单是后者的使用场景。

规格要求产品自身内置耳机曲线查询、播放器音频采集和当前歌曲识别能力。毁 HiFi 与网易云的开放 API 不是实现前提；可以使用网站页面和播放器界面完成数据获取及歌单操作。

已确认的要求：

- 以真实数值与信号处理作为匹配依据。歌名、歌手、流派、评论及大模型语义标签不参与声学匹配评分。
- 支持为耳机生成歌单，也支持从网易云已有歌单中筛选并另建子歌单。
- 支持识别当前播放歌曲，在“我拥有的耳机”范围内推荐并展示适配明细。
- 超高频是独立分析对象，保留实际有效带宽；不能默认降采样后忽略它。
- MEMS 等单元类型可作为设备资料，排序依据是整机测量结果，不能因单元名称直接加分。
- 产品直接具备上述能力。当前 Codex 的浏览器、命令行和计算机操作工具，只用于开发验证，不是终端用户的运行依赖。
- 第一副实际验证耳机为 ARTPICAL Raphael（拉斐尔）。其具体曲线、测量条件和可用歌曲音频尚未取得并验证。

## 2. 目标用户、两个模式与首版范围

目标用户是使用网易云、拥有一副或多副耳机、希望根据实际声音表现选歌或选设备的 Mac 用户。

| 主模式 | 用户输入 | 可观察结果 |
| --- | --- | --- |
| 根据歌曲找耳机 | 当前播放歌曲，或本地已导入/已采集列表中的指定歌曲；我的耳机列表 | 对该录音比较用户拥有的耳机，展示适配排名、频段与时间段依据，以及整曲/片段状态 |
| 根据耳机找歌曲 | 指定耳机配置、候选曲库或网易云已有歌单、期望数量 | 筛选适合评估的歌曲并展示依据，生成新歌单；候选范围为已有歌单时生成其子歌单 |

“根据歌曲找耳机”默认可跟随当前播放，也可固定查看用户选中的歌曲。歌曲尚无可用音频特征时，先进入待采集状态，不能凭元数据产生名次。候选耳机始终限定为用户拥有的耳机。

“根据耳机找歌曲”内提供特色探索、综合适配、超高频适配等排序选项；它们不作为额外主模式。选歌与生成子歌单共用这一入口、数据与计算链路。

首版支持 macOS 14.2 及以上，播放器适配优先网易云原生客户端。毁 HiFi 查询使用应用内浏览器；网易云导入、播放控制和歌单导出优先复用客户端可访问界面。某一步必须依赖网页时，使用应用内网易云页面并保留用户正常登录流程。

首版包含被动采集和用户主动启动的歌单采集队列。被动采集跟随用户听歌；队列采集可以切歌，用户在启动时看到这一行为，暂停或接管播放器后队列让出控制。

“根据耳机找歌曲”的搜索范围是用户选定的候选歌曲集合。尚未采集的歌曲需要先播放采集，不能声称已覆盖网易云全曲库。生成结果可以少于期望数量，必须显示实际可评估数量与缺失原因。

本轮不扩入：播放器替代品、自动修改系统或耳机 EQ、云端音频托管、全平台爬取、歌词语义推荐、跨用户协同推荐、其他操作系统，以及仅凭频响推断声场、瞬态、失真等能力。

## 3. 科学性与结果含义

### 3.1 三类信息分开记录

1. **测得或采集到的数据**：耳机 Hz—dB 数据点、PCM、采样格式、歌曲时间区间。
2. **确定性计算结果**：时频功率、频段能量、相对参考响应的频谱改变、目标偏离量。
3. **推荐推断**：依据这些量对歌曲或耳机排序。算法与参数明确、版本化，但首版排序属于待试听验证的工程模型，不是已被公认验证的“HiFi 匹配标准”。

“某频段能量更大”“两条曲线更相似”“超高频延伸更远”均不能单独定义为更好听。幅频响应只能支持对应的线性频谱分析；缺少相位、失真、不同声压测量时，不输出那些维度的结论。

### 3.2 参考响应与可比性

- 耳机曲线必须明确是原始响应还是已补偿响应。已补偿数据需记录补偿目标，禁止重复补偿。
- 参考响应 `T(f)` 必须和测量设备、耦合器及曲线坐标体系兼容。参考曲线由数据适配层绑定并在详情中可查，不要求普通用户自行调算法参数。
- 首版使用明确、可追溯的参考目标；该目标表达评价基准，不作为唯一正确的听音偏好。
- 不将毁 HiFi“站内各频率最大值组成的统计学参考线”自动视作经过听音验证的偏好目标。
- 未知测量体系、未知补偿方式或缺少适用参考时，可展示原始曲线与歌曲频谱，不产生假装可比的“最佳耳机”名次。
- C 也依赖相对参考响应 Δ；缺少适用参考时，特色探索同样不可计算，不能直接拿未补偿的原始 H 替代 Δ。参考数据必须记录具体名称、测量体系、原始点、来源和修订版，不能只存一个目标名称。
- 跨测量体系的数据不通过简单平移后混排。只有具备可追溯转换依据时才纳入同组计算。
- 测量误差、佩戴与个体耳道差异保留在结果边界中；不显示没有实测依据的置信百分比。

### 3.3 “特攻”和“最佳”的首版定义

首版同时提供两个维度：

- **声学适配**：在该录音实际占用的频段中，相对选定参考响应的加权偏离程度。
- **特色呈现**：该耳机在哪些频段、哪些时间位置造成了明显的增强或削弱。该量表示变化，不自动等于音质收益。

“根据耳机找歌曲”默认使用“特色探索”排序：优先发现该耳机会明显改变哪些歌曲的频谱，列作体现特点的试听候选，而不是把变化大宣称成更好听。同时支持“综合适配”和“超高频适配”排序，比较相对参考目标的偏离。三者共用真实音频和耳机响应计算，不用语义分类代替。

“根据歌曲找耳机”中的“最佳适配”指参考目标、有效频段与已拥有耳机范围内的相对结果。综合适配与 10–20 kHz 适配分别计算；两者相互冲突时展示取舍，不将高频差异仅留在文案里。不能为了产生不同推荐而人为保证每副耳机都有冠军曲目。

“特攻”作为产品用语，不能遮盖这两种不同目标。初版尚无已验证的、能从频响和歌曲自动判断“某种染色对用户有益”的模型，界面必须标明探索候选或参考适配。若之后加入这种收益模型，需要实际试听数据，并保持声学输入与推断依据可追溯。

只有一副可评估耳机时，提供歌曲适配排序和变化明细，不虚构与其他耳机的相对优势。没有足够依据分出优劣时并列；没有数据的耳机单列为无法评估，不记零分。

## 4. 耳机查询与“我的耳机”

### 4.1 查询与保存

用户输入型号或打开毁 HiFi 型号页面。工具检索页面、展示匹配型号，读取页面正常公开加载的数值曲线。优先提取数值，保留曲线用途、单位、来源 URL、抓取时间和必要的原页面证据。

区分频响、THD、相位、阻抗和动态线性偏离等图表，不能因都使用频率横轴而混用。不得把参考线、左右声道平均线或对比耳机误认为目标耳机的实测线。

当前已确认网站前端同时包含 ECharts 数值绘图和图片类内容，下载逻辑包含 PNG 导出；这不等于已经获得拉斐尔的原始曲线或存在稳定公开导出接口。

首版数据入口：

- 从可访问页面取得可核验的数值点。
- 导入用户已有的 CSV/TXT 测量文件，并映射 Hz、dB、声道和曲线类型。
- 图片可附作来源证据和预览，首版不以自动读图数值冒充原始实测。图片数字化算法不纳入本轮必须实现范围。

遇到登录、验证码、页面改版或缺失数据，显示具体原因并保留查询上下文。遵循页面现有访问权限，不绕过登录、付费或访问控制。页面提取失败不会影响已存的曲线与本地分析。

### 4.2 耳机配置

“耳机型号”与“实际使用配置”分开：同型号不同耳套、滤嘴、调音开关、连接模式或 EQ 可以对应不同配置。只要求填写影响当前所选测量对应关系的信息，其他可选。

每个配置至少关联：型号、曲线修订版、测量条件、参考响应、启用 EQ 或未知状态、有效频段。修改配置生成新修订版，使旧结果可解释并按需重算。

用户启用已知数字 EQ 时，其响应作为配置的一部分参与计算。未知 EQ 或无法描述的设备处理须显示“配置不完整”，不能以裸机曲线假称实际设备预测。

## 5. 歌曲识别与音频采集

### 5.1 当前歌曲识别

优先读取网易云界面正常暴露的当前歌曲链接、歌曲 ID、标题、艺人、专辑、版本、时长、播放状态及进度。应用使用自己的界面适配能力，不依赖 Codex，也不假定系统存在可读取所有其他应用曲目的通用 Now Playing 接口。

识别状态：

| 状态 | 条件 | 可进行的操作 |
| --- | --- | --- |
| 已绑定平台曲目 | 有网易云歌曲 ID，且当前播放信息与采集上下文一致 | 当前分析、曲目归档、参与网易云导出 |
| 候选曲目 | 只有部分元数据或来自其他来源的指纹候选 | 分析本地片段，显示候选；取得准确链接或用户绑定后才能映射到网易云 |
| 未识别 | 无足够信息 | 可查看实时频谱，不把音频归入另一首歌 |

音频指纹仅作为识别辅助，不证明母带、音质或 DSP 状态完全一致。首版不要求建设外部指纹识别服务。

网易云歌曲 ID 是平台曲目身份，不保证音频字节或母带永久不变。特征绑定实际采集样本、音质及处理状态。同名、同艺人、相同时长均不足以自动合并不同录音。

### 5.2 数字音频捕获

使用 Core Audio Process Tap 捕获选定的网易云进程或经验证的音频进程组，取得 PCM。保持正常播放，不默认静音，也不默认捕获整个系统或麦克风。

采集器必须运行时读取采样率、声道数、样本格式、时间戳和数据连续性。优先保留收到的立体声浮点格式，不默认为单声道，不先降采样为语音音质。

曲目识别和 PCM 时间戳需要关联，不能只按窗口标题切换时刻拼接音频。切歌过渡无法明确归属的样本不计入任一曲目的完整覆盖。

| 播放事件 | 采集行为 |
| --- | --- |
| 正常播放 | 追加当前曲目的连续片段，记录媒体时间区间 |
| 暂停 | 停止累计有效覆盖；恢复后建立新片段并核验曲目与进度 |
| 拖动或跳播 | 结束当前连续段，按新进度开始另一段，不把跳过区间视为已采集 |
| 切歌或交叉淡入淡出 | 结束旧段；混合过渡标记为不可用于单曲特征，待新曲身份稳定再采集 |
| 输出设备或采样格式变化 | 切分采集段，重新读取格式；不得直接拼接不同格式 PCM |
| 用户接管队列播放 | 队列暂停，保留进度，不反复抢回播放器 |
| 权限撤销、进程退出、丢帧或持续异常零数据 | 结束或暂停采集，说明原因，已有有效片段可保留 |

静音段可以是音乐内容的一部分，不能仅因能量低就判为采集失败。异常零数据需结合播放进度、设备回调和连续性判断。

### 5.3 处理状态与采集质量

采集的是播放器输出的数字信号，不保证等同于原始母带。每次记录音质设置，以及播放器 EQ、音效、响度均衡、空间处理、可见系统处理的开启/关闭/未知状态。

首版可自动读取到的状态自动读取；读取不到时，允许用户按播放器配置一次声明，后续不能把声明写成已测事实。可见状态变化使对应配置失效。

- 固定整体增益变化可以在相对分析中消除，原始值仍保留。
- 知道已经应用的 EQ 时，不能再次叠加同一个 EQ。首版不默认反演未知处理。
- 有耳机专属 EQ、空间音效或其他未知频谱处理的采样，可显示“基于当前播放处理的结果”，不归入通用原始录音特征缓存。
- 面向“换另一副耳机”的一般推荐，使用处理状态明确、可供候选耳机共同比较的样本；当前耳机专属处理未处理清楚时，说明该推荐的限制。

### 5.4 完整与片段

用曲目媒体时间上的有效区间并集计算覆盖，重播和重叠片段不重复增加完整度。不得简单用录音总时长除以歌曲时长。

只有准确时长、起止覆盖、无未解释断点、无混曲且身份一致时，标记“整曲”。首尾编码延迟或进度采样容差必须写入分析参数；初值每端不超过 250 ms，超出显示实际遗漏区间。内部缺口不能被首尾容差抵消。

无法读取准确进度或版本身份时，只标记采集时长与片段，不能宣称整曲覆盖。当前播放初期可显示片段结果；第一段累计 15 秒有效非静音内容后生成暂定推荐，此阈值是交互初值，不是统计可靠性保证。

整曲与片段结果不混成同一排行榜。首版默认用整曲生成最终子歌单，用户可显式切换为“按已采集片段生成”，结果保留该标识。

## 6. 时频分析与高频契约

### 6.1 提取内容

输入为可追溯的 PCM 片段及采样元数据，输出至少包括：

- 分声道的频率轴、媒体时间轴与功率谱密度；峰值、有效样本和缺失区间。
- 可重新汇总的频带能量、持续占用、分位数及局部高能量时间段。
- 整曲或片段响度、整体电平与动态变化摘要。
- 原音频采样格式、采集处理状态、内容哈希、分析器及参数版本。

使用 Hann 窗 STFT。首版参数取约 180 ms 的最近 2 次幂窗长，步长为窗长的 1/4，按实际采样率计算；例：44.1/48 kHz 使用 8192 点，96 kHz 使用 16384 点。较长窗与较短窗的时频分辨率取舍明确记录；不能用零填充宣称提高真实频率分辨率。

上述窗长用于主时频计算，不保证能分辨所有细分频段或窄峰。细分明细必须同时记录频率间隔、窗函数与窗长；频段过窄或测量点过疏时标记“分辨率不足”，不能靠插值制造细节。需要更细的音频分析时，使用更长的真实连续样本窗并记录新的时间分辨率；没有足够连续样本或原始 PCM 已清理时，保留限制或重新采集。局部重算结果单独保存参数，不静默替换主排序所用特征。

先对线性 PSD 应用耳机响应，再汇总为分析频带。积分考虑实际频率间隔和窗函数能量；禁止把幅度直接当功率，或直接平均 dB 当作平均能量。左右声道分别处理，不能先相加波形导致相位抵消。

提取参数禁止使用音频库默认的 22,050 Hz 重采样或默认 mono。计算中允许明确目的、保留参数的重采样，但不增加有效带宽。

缓存必须足以在声明的分辨率下对新耳机重新计算。首版优先保证可复算，显示实际磁盘占用并允许清理；若对谱矩阵压缩或降低时间分辨率，需要对原始计算结果验证误差，不能静默更换表示。

### 6.2 PEQ 粒度展示与扩展频段

展示细分到便于理解 PEQ 局部影响的粒度。PEQ 通过中心频率、增益及 Q/带宽描述滤波器，本身不是一组固定频段。产品默认采用约 1/3 倍频程的对数细分，详情支持选择任意中心频率与带宽查看局部证据；这属于分析视图，不新增 EQ 调节或自动写入设备的功能。

以下数值明确用作**展示区间边界**，相邻两点构成一个频段；它们不是 PEQ 中心频率列表，也不声称实现了标准 1/3 倍频程滤波器组：

| 展示范围 | 相邻区间边界 |
| --- | --- |
| 20–200 Hz | 20、25、31.5、40、50、63、80、100、125、160、200 Hz |
| 200 Hz–2 kHz | 200、250、315、400、500、630、800、1,000、1,250、1,600、2,000 Hz |
| 2–20 kHz | 2、2.5、3.15、4、5、6.3、8、10、12.5、16、20 kHz |
| 20 kHz 以上 | 20、25、31.5、40、50、63、80、100 kHz；仅按有效数据范围延伸，超过 100 kHz 时继续同一十倍频率序列 |

因此，10–20 kHz 展示为 **10–12.5、12.5–16、16–20 kHz**；扩展频段分别展示 **20–25、25–31.5、31.5–40、40–50 kHz** 等区间，不能合成一个没有内部明细的“20 kHz 以上”总栏。低、中、高频概览可折叠汇总这些区间，但不能替代细分明细。

每段显示实际上下限、有效覆盖范围、歌曲能量及占比、耳机相对增益与偏离贡献，并可定位歌曲中的时间区间。没有音乐内容、没有数据、仅部分覆盖和分辨率不足分别表示。能量与贡献从底层 PSD 积分得到；默认区间采用左闭右开、最末有效端点包含的规则，边界频率不重复统计。自选局部窗口可以重叠，但不与默认分段再次相加。

展示边界与 FFT 栅格不重合时，以相邻栅格中点划定频率单元，按单元与区间的实际重叠宽度分配积分权重；主计算掩码和明细汇总使用同一约定。不把最近的 FFT 点直接当作区间边界，也不把这种数值积分近似解释为额外的真实频率分辨率。

自选分析窗口以几何中心频率 `f0` 和倍频程带宽 `B` 表示：`f_low=f0×2^(-B/2)`，`f_high=f0×2^(B/2)`，同时显示实际 Hz 上下限。此窗口不是 PEQ 滤波器传递函数；不能把窗口宽度直接当成任意 PEQ 类型的 Q，或把区间外的滤波器影响视为零。

展示细分与计算分辨率分别记录。调整分段或局部查看窗口只改变证据汇总，不改变 C/D/D_high 的原始计算、固定比较频率掩码或默认排名；尤其 D_high 仍覆盖完整的 10–20 kHz，不能用三个子段的 dB 值简单平均代替。

每个样本及曲线分别记录有效频率范围。一次计算使用共同有效范围：

`有效范围 = 音频可分析范围 ∩ 采集链路有效范围 ∩ 耳机测量有效范围 ∩ 参考响应有效范围`

- 44.1 kHz 的理论奈奎斯特频率是 22.05 kHz，48 kHz 是 24 kHz，96 kHz 是 48 kHz；可用范围还受抗混叠滤波、编码及真实音源带宽影响，不把理论上限等同于有效信息上限。
- 10–20 kHz 必须有独立明细，不能因整曲低频能量较多就不展示该段贡献。
- 20 kHz 以上保留可验证的数据与响应预测，但首版不将其直接计入“更好听”的默认总排序；没有经过验证的感知模型，不因超声能量多而奖励。
- 未覆盖的频段显示“无数据/不可评估”，不能补零，也不能把它当成耳机能力弱。
- 有效上限落在某个展示区间内部时保留其标称区间，同时显示实际可评估部分。例如共同有效上限经确认只有 24 kHz，则 20–25 kHz 段只报告 20–24 kHz，标记“部分覆盖”；不估算 24–25 kHz，也不拿它与完整 20–25 kHz 结果直接比较。实际信息上限未确认时不只凭采样率宣称覆盖完整。
- 传统耦合器高频重复性、佩戴与插入深度影响须体现在测量条件和有效范围中。绘图坐标画到 40 kHz 不代表测量有效到 40 kHz。
- 排行榜先按测量体系与参考分组，再固定频率掩码，不能给每副耳机单独缩短掩码。默认主榜要求 20–20,000 Hz 范围内有可靠的音频、参考和耳机测量数据；不覆盖该范围的耳机单列，不能加入后把现有主榜一起降到 10 kHz。
- 当输入或耳机只支持受限范围时，生成明确标注端点的辅助比较组。组内取该组音频、参考及候选曲线有效范围交集，冻结掩码并保存候选集合；其名次不与主榜混排。加入一副受限耳机不能改变既有完整范围结果。
- 一个源歌单的同一次排序使用固定频带；不因歌曲音源格式不同而逐首切换 D 的计算范围。不能满足该范围的歌曲另列。超高频名次始终要求完整的 10–20 kHz 数据，不能用 10–12 kHz 的计算冒称整个超高频。
- 若只有一副耳机具备所需范围，显示“仅这一副有完整可评估数据”，不把其余缺失数据解释成落败。
- 单次录音的谱截断只能作带宽线索，不足以证明音源格式或母带带宽。未知处保持未知。

数字 LUFS 归一化用于比较，不等于声压校准。未获得实际播放声压时，首版不输出绝对耳内 SPL、听阈、可听性概率或 ISO 感知响度结论。

## 7. 匹配模型 V1

### 7.1 物理计算

令 `P_s,c(f,t)` 为歌曲 s、声道 c 的线性 PSD。耳机配置的频响 `H_h,c(f)`、参考 `T_c(f)` 和已知但未包含在采集中的 EQ `Q_h,c(f)` 均以 dB 表示。

保留原始曲线。在共同有效频率内按 log(Hz) 坐标对 dB 做线性插值，不向外外推；不自动抹平窄峰。图示对齐使用 500–1,000 Hz 对数等距点上与参考差值的中位数，两个声道使用同一个偏移量；该段缺失时不强行图示对齐。计算中的整体增益按下节 g 统一消除，对齐参数随结果保存。

`Δ_h,c(f) = H_h,c(f) + Q_h,c(f) - T_c(f)`

`P_relative,s,h,c(f,t) ≈ P_s,c(f,t) × 10^(Δ_h,c(f)/10)`

这是线性、相对参考的频谱估计，不是实际耳道声压，也不模拟非线性失真。未知相位不影响这个功率增益计算，但不能据此宣称重建了完整听感或瞬态波形。

### 7.2 适配与特色明细

首版基线使用曲目能量加权的参考偏离量。令 `w_s,c(f,t)` 是在共同有效范围内归一化的歌曲能量权重，离散计算为 `P_s,c(f,t) × Δf × Δt / Σ(P_s,c(f,t) × Δf × Δt)`。求和仅覆盖有效样本，重叠 STFT 窗按同一归一化约定处理；总能量为零时返回无法评估，不执行除零或产生名次。

基线允许为每个“歌曲×耳机”组合选择一个全局电平偏移，再衡量剩余频谱形状差异。这个拟合项不是实测播放增益，也不是 LUFS 或感知响度匹配：

`D(s,h) = sqrt(Σ w_s,c(f,t) × (Δ_h,c(f) - g_s,h)^2)`

其中 `g_s,h = Σ w_s,c(f,t) × Δ_h,c(f)`。`D` 越小，表示允许这个全局电平调整后，该歌曲实际占用频段中的剩余谱形偏离越小，单位为加权 dB；不将它解释为失真 THD、主观音质分或用户喜欢概率。

这一定义有意不奖励或惩罚单纯响一点：若歌曲只有一个频率，或其全部能量处于同一平坦增益区，所有耳机可能均得到 D≈0。此时只有电平差异证据，不能据此宣称它们音质相同或某副耳机特攻；界面显示“当前内容不足以区分谱形”。验收必须覆盖这一退化情形。

同时输出每个展示频段的：输入能量占比、相对增益变化、偏离贡献、局部峰谷与对应的歌曲时间位置。只在信号确有相应内容时解释该段影响；不把无内容频带的噪声放大解释为音乐细节。

为避免 10–20 kHz 被较大的低频能量淹没，另计算该范围内独立归一化的偏离量：

`D_high(s,h) = sqrt(Σ_high w_high,s,c(f,t) × (Δ_h,c(f) - g_s,h)^2)`

`w_high` 只在该高频范围内归一化，但仍使用全频同一个 g，不能重新消除高频整体增益而藏掉高频抬升。D 与 D_high 分别报告，不合成一个任意权重的“音质分”。

若高频能量占有效全频能量小于 10^-4，暂不以 D_high 产生独立名次，显示“该样本高频能量不足以支持此项排序”。该阈值是避免极弱残留或数值噪声被独立归一化放大的工程初值，不是人耳听阈；高频测量不完整与高频内容不足是不同状态。实际听觉频带或感知权重的引入需对应试听证据，并发布为新的算法版本。

### 7.3 特色探索量

对每个有效时间帧，把各频率与声道的输入功率积分归一化为分布 p，把应用 Δ 后的相对输出功率归一化为分布 q；两者使用同一频率掩码，整体增益不会改变分布。

计算 `C_frame = JS(p,q)`，其中 `m=(p+q)/2`，`JS(p,q)=0.5×KL(p||m)+0.5×KL(q||m)`，采用以 2 为底的对数并按标准极限处理零项。整曲或片段的 C 是按各帧输入能量归一化加权的 C_frame 均值，避免静音的数值残留被等权放大。

C 是归一化谱分布的变化量，范围为 0–1 bit，不是喜爱概率、置信度、感知距离或音质收益。它同时对增强与削弱敏感，结果必须伴随 D/D_high 与带正负方向的频段明细。高 C 可能意味着明显而不悦耳的染色，只称“特色明显的试听候选”。

这是首版对“发挥特点”的可测替代指标，是否符合用户实际感受到的耳机特色需要试听检验。不能拿 C 最大的耳机作为当前歌曲的“最佳适配”。

### 7.4 双向排序

- **根据歌曲找耳机**：对当前播放或用户指定的歌曲，在“我的耳机”可比配置集合中，同时比较 D 与可用的 D_high。若某耳机两项都不差、至少一项更好，则优先于另一副。优先展示未被其他候选同时超过的耳机；综合更好而高频更差时并列展示取舍，不强造唯一最佳。同层按 D、D_high、本地稳定 ID 排列。D_high 对整个比较组不可用时，仅按 D，并明确未比较高频维度。
- **根据耳机找歌曲—特色探索排序（默认）**：按 C 从大到小找出体现该耳机频响特点的候选，数值相同保持源歌单顺序；同时展示 D/D_high 和变化方向，不能以“适配最佳”标注此序列。
- **根据耳机找歌曲—综合/超高频适配排序**：若拥有至少两副可比耳机，综合相对优势 `A(s,h) = median(D(s,其他耳机)) - D(s,h)`；超高频相对优势用相同方式从 D_high 得到 A_high。所选优势越大越优先，相同时按该维度偏离量从小到大、源歌单顺序排序。没有可用 D_high 的歌曲不进入超高频适配结果，但仍可参与综合适配。
- **仅一副可比耳机**：特色探索仍用 C；参考适配按所选维度的 D 或 D_high 排序，并明确没有横向设备比较依据。
- 不把固定耳机的最大频响偏离量当成“擅长程度”。明显染色可以成为特色说明，但不单独作为适配奖励。
- 首版用于分层比较的 D/D_high 先按固定原点 0、步长 0.5 加权 dB 四舍五入分档，处于同档时显示“接近”，数值明细保留未分档结果。这是避免细小数值差异被过度解释的工程初值，不是声学可辨阈值；不能将不同单位的夹具误差直接加到 D 上。数据不足以支持分档比较时改为展示取舍或不可评估。
- 排名可出现一副耳机在全部候选歌曲中领先，也可全部并列；禁止为了多样性修改声学结果。

若来源提供同一配置的重复测量，逐份重算 C/D/D_high/A，保存观察到的结果范围；范围重叠的候选不能宣称稳定领先。只有单次曲线时标记“测量变动未量化”，排序属于点估计。不同夹具宣传的公差、一次测量的频段误差上界和重复测量范围不是同一概念；首版不把它们未经推导直接换算成推荐分误差。

这是最小可复算基线。特色探索检验“变化是否明显”，参考适配检验“允许电平调整后的谱形是否接近目标”，两者均不能证明所有用户更喜欢结果。试听验证若不支持预期，需修改模型及结果命名，不能通过推荐文案掩盖差异。

### 7.5 解释生成与 AI 边界

结果首先生成结构化证据：输入数据版本、参考、有效频带、每段贡献、时间位置和限制。大模型仅可将这些证据转写为易懂说明；无模型或模型不可用时，模板说明仍应完整可用。

不允许编造某段乐器、母带质量、齿音或“更高级的解析”。只有频谱证据时描述频段变化；需要乐器识别、失真测量等才能成立的解释不输出。AI 不决定名次，不补齐缺失频响，不代替实际音频分析。

## 8. 网易云歌单导入与输出

### 8.1 导入

导入保存源歌单 ID、URL、名称、读取时间、顺序以及歌曲 ID/元数据。处理分页和虚拟列表；分别显示已读取、未解析、不可播放及待采集数量。

保留源快照顺序和原始重复行，候选分析可按平台曲目与音频版本去重。最终子歌单默认每个网易云歌曲 ID 只保留一次，不能换成同名翻唱、现场或重制版本来凑数。

导入中断时保留已读快照并标记不完整，不把当前可见十几行当作完整歌单。用户可继续读取或基于明确的已导入子集操作。

### 8.2 生成与编辑

在“根据耳机找歌曲”中，用户选择耳机配置、候选范围、排序选项（特色探索/综合适配/超高频适配）与数量。生成结果显示已评估范围、整曲/片段依据、推荐顺序及明细；允许移除、保留或重新排列歌曲。用户编辑影响导出内容，不修改原始声学排名记录。

“子歌单”是另建的网易云歌单，本地记录与源歌单的派生关系；不假设网易云存在父子歌单原生结构，不修改源歌单。

### 8.3 写入与回读

点击“保存到网易云”即授权本次明确预览内容的写入，不增加重复确认流程。通过网易云正常界面创建并添加曲目，记录目标歌单 URL/ID及每步状态。账号发生变化时停止当前写入，防止保存到错误账号。

导出前保存操作记录：账号、本次结果修订版、期望曲目 ID 与顺序、已有目标歌单 ID。重复点击同一次导出复用记录；用户明确选择另存新歌单时才创建新操作。

发生超时或无法确定创建/添加是否成功时，先读取目标状态，不盲目再次创建或重放全部添加操作。界面操作不能承诺原子事务或严格 exactly-once。

导出状态至少区分：尚未写入、写入中、部分写入、已创建待核对、已核对、失败。只有重新打开目标歌单并核对实际歌曲 ID、去重后数量与顺序符合预览时，才显示“已保存并核对”。

若平台界面只能验证集合而不能验证顺序，明确标记顺序未核对。无法取得准确 ID时，不以相同歌名充当核验。失败时展示已保存部分、缺失曲目及目标链接，支持继续处理同一目标；不自动删除用户歌单作为回滚。

## 9. 页面与日常交互

首页以两个主模式组织操作：

| 主入口 | 主要内容 |
| --- | --- |
| 根据歌曲找耳机 | 选择跟随当前播放或指定歌曲，展示已有耳机的适配排名、频段明细、整曲/片段状态 |
| 根据耳机找歌曲 | 选择耳机和候选曲库/歌单，查看采集进度、选择排序、预览结果并保存到网易云 |

“我的耳机”是两个模式共用的管理入口，负责查询/导入曲线、配置与拥有列表；“数据详情”从结果打开，展示曲线、谱图、来源和计算依据。二者均不作为第三或第四个推荐模式。“正在播放”属于根据歌曲找耳机的输入状态，歌单导入和导出属于根据耳机找歌曲的流程。

切换模式保留各自选中的歌曲、耳机与候选范围，不自动切歌或启动采集队列。根据歌曲找耳机处于“指定歌曲”状态时，播放器切歌不替换用户正在查看的歌曲；处于“跟随当前播放”状态时才随曲目更新。

歌曲结果使用完整缓存时明确显示缓存依据；音频版本或处理状态不一致时不能直接复用旧名次。跟随当前播放时，新曲开始后清除上一首的名次绑定，不在旧歌标题下继续更新新歌频谱。

暂定结果随新片段更新，但最多每 5 秒刷新一次排序，避免逐帧跳动；该节流不改变底层数据计算。未知曲目、等待有效片段、不可比耳机和权限缺失都有明确空状态。

采集状态必须持续可见，并有随时停止入口。退出应用终止 tap 和应用创建的临时聚合设备；不留下默认系统回环录音。

## 10. 最小数据契约与本地架构

### 10.1 数据对象

| 对象 | 必需信息 |
| --- | --- |
| HeadphoneProfile | 本地 ID、品牌型号、拥有状态、实际配置、曲线修订版、EQ 状态及修订版 |
| CurveRevision | 原始点/声道、单位、测量体系、曲线类型、补偿目标、来源证据、有效范围、原始数据哈希；重复测量引用、来源给出的分频段误差及其定义，缺失明确为 unknown |
| ReferenceRevision | 参考名称、原始数值点、单位、适用测量体系、有效范围、来源、数据哈希及修订版 |
| Recording | 本地录音版本 ID、网易云曲目 ID（可空）、元数据快照、版本/音质、识别状态 |
| CaptureSession / Segment | 播放器身份、媒体区间、墙钟与音频时间戳、采样格式、处理状态、丢帧/过渡标记、音频哈希 |
| FeatureArtifact | 样本引用、覆盖区间、PSD与摘要、频率/时间轴、有效带宽、分析参数及版本 |
| MatchResult | 歌曲特征/耳机/参考修订版、固定频率掩码及比较组、C/D/D_high/A/A_high及各频段证据、展示分段版本/边界/实际覆盖/分辨率状态、自选窗口与局部分析参数、整曲/片段范围、重复测量的结果范围或未量化标识、模型版本 |
| PlaylistSnapshot / ExportOperation | 源快照、选中曲目与顺序、目标账号与歌单、操作状态、写后核验明细 |

平台歌曲 ID、音频版本 ID、采集 session ID 不混为一个字段。缓存键必须覆盖实际音频样本/处理状态、特征版本、曲线和 EQ 修订版、参考响应、比较频带及模型版本。单对声学计算与拥有耳机集合上的排名分开缓存；A/A_high及名次还绑定本次候选耳机配置集合，增删耳机后重新计算相对结果。

### 10.2 建议实现取向

- 原生 SwiftUI 外壳；Core Audio 负责 PCM；WKWebView 负责站内查询；macOS Accessibility 负责网易云原生客户端的可访问界面操作。
- 音频分析优先复用 librosa/SciPy 与 pyloudnorm，作为随应用交付的本地工作进程；最终用户不需要安装 Python、ffmpeg 或 Codex。若实现时改用原生 DSP，仍须满足相同数值契约和对照样例。
- SQLite 保存实体、版本和任务状态；谱矩阵及来源文件存应用本地数据目录。首版不引入服务端、云队列或用户账号体系。
- 采集、分析和界面操作分开，分析计算不阻塞音频回调。采用有界缓冲；跟不上时记录丢帧并停止声称完整，不无界堆积内存。
- 网站/播放器适配与分析核心隔离到少量明确模块即可，不建设通用插件框架或多平台自动化平台。

本地工作进程的输入是受限的文件路径及结构化分析参数，输出特征文件和结构化结果；不得执行网页或播放器文本提供的命令。

### 10.3 保存与清理

原始 PCM 默认只保留到特征成功写入且可读校验完成；分析失败时保留对应临时样本供重试，并显示占用。用户可选择保留原始采样以便后续重算，也可清理样本或特征。

删除原始 PCM 后，缓存特征只支持其现有分辨率及模型所需信息；新增分析维度可能要求重新采集，不能伪造缺失信息。清理正在被分析或导出引用的数据时停止相应任务并说明影响。

## 11. 权限与数据使用

- 首次采集时由系统申请系统音频录制权限，配置 `NSAudioCaptureUsageDescription`。它不是麦克风权限。
- 操作网易云原生界面时按需申请辅助功能权限。应用内网页查询本身不要求获取整个桌面的辅助功能或录屏权限。
- 首版优先可访问界面与页面数值读取，不以全屏截图/OCR作为默认采集方式；确需屏幕读取的功能再单独说明权限用途。
- 用户通过网易云、毁 HiFi 的正常页面登录；工具不要求用户手动粘贴账号 Cookie，不跨站发送凭据。
- 音频与分析默认留在本地。可选 AI 解释只接收完成解释所需的结构化证据，不默认上传音频或账号信息。
- 用户停止采集、退出应用或撤回系统权限后，停止接收音频；网站改版或登录失效不会触发其他来源的隐式抓取。

## 12. 验收与验证样例

验收分开记录数据获取、数值计算、界面流程、网易云写后结果和试听效果。应用能展示曲线不等于已读取可计算数值；能收到 PCM 不等于已经获得干净、完整、版本正确的录音；歌单创建成功不等于全部曲目已写入。

| 场景 | 期望证据/结果 |
| --- | --- |
| 两个主模式 | 首页提供根据歌曲找耳机、根据耳机找歌曲；前者只比较已有耳机，后者覆盖选歌与子歌单生成；排序选项不冒充额外模式 |
| 跟随与指定歌曲 | 跟随状态随播放曲目更新；指定歌曲状态保留所选歌曲；模式切换不自动切歌或启动采集 |
| 拉斐尔型号页 | 确认型号、曲线类型、数值点、测量条件与有效范围；缺哪项就明示哪项 |
| 数值曲线导入 | 源数据与本地曲线叠图一致，保留原点与处理参数；曲线排序、重复频率和非有限值有明确处理 |
| 双声道与采样率 | 使用已知声道不同的信号，确认未合单声道，记录实测格式；高采样率不被默认降采样 |
| 频带幅度计算 | 已知频率信号配合 +6 dB 响应，线性功率变化接近 10^0.6；单位与积分正确 |
| 无音色差异 | 耳机等于参考时 Δ 为零；仅整体增益变化不改变适配名次 |
| 窄带退化 | 单频及全部内容落入同一平坦增益区时 C/D 可同时接近零，显示不足以区分谱形，不冒充已证实音质相同 |
| 特色探索与适配 | 增强及削弱都可增加 C；高 C 不自动成为最佳适配，根据耳机找歌曲内的各排序选项使用对应计算量 |
| 双向排序 | 合成两副不同频段偏离的耳机和至少含两组频率成分的歌曲，排序由对应频段决定；相同响应保持并列，增删候选耳机重算相对优势 |
| 高频取舍 | 综合偏离较小但高频偏离较大的候选不直接压过两项取舍不同的耳机；超高频选歌确实使用 D_high/A_high，极弱高频不被无限放大 |
| 超高频 | 使用已知 12/16/19 kHz 内容检查保留；扩展频段以真实高采样率样本验证，不把升采样当作新信息 |
| PEQ 粒度明细 | 默认显示约 1/3 倍频程细分，并支持中心频率/带宽局部查看；分段加总与同范围底层积分一致，边界不重复统计，改变展示粒度不改变原始评分和排名 |
| 窄带分辨率 | 用已知相邻频率信号检查主窗无法分辨时的提示；长窗重算使用真实连续样本并记录时间分辨率，不以零填充或插值冒充独立细节 |
| 20 kHz 以上分段 | 分别显示 20–25、25–31.5、31.5–40、40–50 kHz 等区间；有效上限截断某段时明确部分覆盖，不补齐或与完整区间混比 |
| 频带缺失 | 无测量覆盖的高频显示不可评估；比较采用相同范围，不因缺失而得更好名次 |
| 排行范围稳定 | 加入只能测到 10 kHz 的耳机不会降低现有 20 kHz 主榜范围；受限组和主榜不混排 |
| 测量变动 | 重复测量分别重算，保存观察结果范围；区间重叠不宣称稳定胜出，unknown 不按零误差处理 |
| 暂停/拖动/切歌/混曲 | 媒体区间与实际PCM一致；重叠不重复累计，跳过与交叉过渡不冒充完整 |
| EQ与音质变化 | 处理状态或实际音频发生变化时不错误命中旧特征；不重复应用 EQ |
| 当前曲识别 | 当前歌曲身份、采集段与显示名次一致；仅候选身份不被静默写成确定歌曲 ID |
| 歌单导入 | 完整读取分页/虚拟列表；未解析行、不可播放行及源顺序可核对 |
| 子歌单导出 | 精确 ID、去重后数量、顺序与用户预览核对；中断重试不盲目新建或全量重复添加 |
| 权限与退出 | 撤销权限、停止、退出后结束采集；其他应用声音未混入指定播放器样本 |
| 可复算性 | 同一输入与版本得到同一结果；修改曲线/EQ/参考/音频版本时重算受影响项 |
| 实际搭配试听 | 用真实拉斐尔与真实录音验证推荐解释；若比较拥有耳机，使用同一录音并匹配响度，隐藏名次后收集偏好 |

首轮试听可选 10–20 首用户熟悉且音频版本明确的歌曲，包含不同能量分布和高频内容。记录支持、无明显区别或反对模型排序的结果，避免只展示成功例子。该数量仅用于发现问题，不宣称统计验证了通用偏好模型。

只有一个耳机时，可以验证数据链路和曲目筛选体验，但不能宣称完成了真实设备之间“最佳适配”的业务验证。试听证据与合成信号计算测试分别报告。

## 13. 当前证据与实现时需落实的依赖

已确认：

- 本轮开发机为 macOS 26.6.2，有网易云客户端、Swift 编译器和 macOS SDK。
- SDK 包含 `CATapDescription`、`AudioHardwareCreateProcessTap`，后者从 macOS 14.2 可用；Apple 提供按进程采集系统音频的实现路径。
- 毁 HiFi 首页可访问，公开前端包含数值绘图及图片测量内容。
- librosa/SciPy 可计算多声道时频数据，pyloudnorm 可做数字响度测量。

尚未验证，不能写成已有产品能力：

- 拉斐尔在毁 HiFi 的准确条目、原始数值、测量设备、适用参考与高频有效范围。
- 首版默认参考目标的具体数据集与修订版，需在确认实际测量体系后选定；未落实前只开放原始数据查看，C/D/D_high/A/A_high 均显示不可评估。
- 授权后是否能稳定采到网易云实际音频进程、采样率和完整片段；是否存在受保护内容无法捕获的情况。
- 网易云当前版本界面是否提供足以绑定精确歌曲 ID、媒体时间和音质/处理状态的信息。
- 歌单完整读取、新建、加歌及写后回读的界面路径和可靠性。
- 实际打包应用在目标系统上的签名、音频与辅助功能权限行为。
- 特色变化量与参考适配基线能否支撑用户期望的“耳机特攻”；两者需要真实试听证据，不能靠功能跑通替代。

这些是具体数据与集成依赖，不增加独立审批或流程门禁。实现应优先围绕拉斐尔、一个真实歌单、两副可比耳机（如用户已有）打通最小链路；遇到实测不成立的部分，修订该契约并明确影响，不扩建无关框架。

## 14. 参考资料

- [Apple：Capturing system audio with Core Audio taps](https://developer.apple.com/documentation/coreaudio/capturing-system-audio-with-core-audio-taps)
- [毁 HiFi](https://huihifi.com/home)
- [本轮读取的毁 HiFi 测评页前端资源](https://cdn.huihifi.com/static/www/src__pages__evaluation__$uuid.e11205dd.async.js)；资源哈希可能随站点发布变化。
- [librosa STFT](https://librosa.org/doc/0.11.0/generated/librosa.stft.html)、[librosa 音频读取默认参数](https://librosa.org/doc/0.11.0/generated/librosa.load.html)
- [SciPy ShortTimeFFT](https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.ShortTimeFFT.html)
- [pyloudnorm](https://github.com/csteinmetz1/pyloudnorm)
- [REW：PEQ 的中心频率、增益与 Q/带宽参数](https://www.roomeqwizard.com/help/help_en-GB/html/eqfilters.html)
- [Sean Olive：The Perception and Measurement of Headphone Sound Quality](https://acousticstoday.org/wp-content/uploads/2022/03/The-Perception-and-Measurement-of-Headphone-Sound-Quality-What-Do-Listeners-Prefer-Sean-E.-Olive.pdf)
- [GRAS：高频耳模拟器及传统 711 测量限制](https://www.grasacoustics.com/products/ear-simulator/product/786-ra0402)
- [GRAS：扩展至 50 kHz 的耳模拟器](https://www.grasacoustics.com/products/ear-simulator/product/809-gras-ra0404-prepolarized-hi-res-ear-simulator)

外部资料支持其对应的测量或工具能力，不自动证明本规格推荐模型有效。所有产品行为以本文契约和后续实际验证记录为准。
