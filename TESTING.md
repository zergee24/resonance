# 本机验证记录 · 0.2.2

日期：2026-09-25—2026-09-26。被测提交：`79db5a7`。macOS 26.6.2，Apple Silicon，Swift 6.3.3，部署目标 macOS 14.2。

## 2026-09-25：0.2.2 真实账号测试

- 使用真实已登录 WebView 和生产 `WebWorkspace` / `PlaylistWriter` 源码编译独立 UI probe，隔离尚未成功的音频采集前置。没有用假录音或猜测歌曲 ID 进入声学匹配；这次证明的是网页写入链路，不是录音到歌单全链路。
- 单曲创建与同请求只读恢复通过。最终双曲请求自动完成创建、逐首添加、目标回读，期望和实读顺序均为 `30854130 → 2083530698`，状态 `verified`；再次执行同请求仍为两首，没有重复创建或加歌。
- 真机修正：隐藏账号菜单中的 `/login` 不再误判为未登录；收藏到歌单使用 `fav` 而不是播放队列 `addto`；歌单使用普通文档 URL，避免 hash 跳转不触发 WebKit 加载回调；等待异步列表出现目标 ID；按反序添加以抵消网易云的首部插入。
- 歌单解析限定主内容，修正侧栏“500首歌曲”污染当前三首歌单总数，以及“我的音乐”页面标题误抓侧栏。真实三首歌单回读名称正确、总数 3、`complete`。
- 只新增 4 个明确标注的测试歌单，保留前面失败的测试现场。原有歌单未选作写入目标，并做了只读内容与数量核对。账号 ID、用户歌单明细、录音和本地 receipt 不提交到仓库。
- `node Tests/WebProbe/playlist-writer.test.cjs` 通过，覆盖隐藏登录项、真实可见登录、未知状态、精确歌曲 ID 与收藏动作。
- 0.2.2 Release 构建、签名验证与正式应用启动通过。可执行文件 SHA-256：`fa3f9c6b905f3918e76d03439c21c8c71b8596637ffc769dccf49e89ea795cc4`。

### 2026-09-25 历史失败记录：真实音频采集当时未通过

两次 GUI 实测均在 `AudioDeviceStart` 超时；进程采样和 Core Audio 日志显示 HAL server `0x10004003`，失败 CAF 只有文件头、0 音频帧。退出共鸣后，独立生产采集 probe 对同一网易云进程也失败；加入默认输出 clock/subdevice 的诊断对照没有恢复。没有凭此修改采集底层或增加备用框架，也未擅自重启系统音频服务。

0.2.0 的 tapautostart 修正使当时的独立 probe 通过，但不能证明当前真实客户端录音已修好。该结论只适用于当时未重启音频服务的现场；当前可导入已有本地音频进行匹配。新增生产 probe 的 `--pid` 参数，便于直接复测指定播放进程。

### 2026-09-26 重启 coreaudiod 后：生产音频采集通过

- 在录音权限已开启的前提下，重启本机 `coreaudiod` 后重新启动同一 0.2.2 生产应用；未修改采集代码、默认音频设备或隐私权限。该次验证确认恢复条件包含干净的系统音频服务状态，不能把“权限已开启”单独视为充分条件。
- 生产 GUI 触发 `AudioCapture.start → process tap/aggregate → IOProc → ring → CAF → stop` 成功。CAF 为 2 声道、48 kHz、Float32 交错格式，时长 8.8 秒，422,400 个有效帧，3,379,200 字节音频数据，文件大小 3,383,296 字节；无 `0x10004003` 或 `StartIO` 错误。
- coreaudiod 日志确认 tap IOContext 注册、客户端进入 `running: yes`，停止时正常注销并释放防休眠 assertion。0.2.2 可执行文件 SHA-256 仍为 `fa3f9c6b905f3918e76d03439c21c8c71b8596637ffc769dccf49e89ea795cc4`。
- 随后连续录音 194.634667 秒，48 kHz 双声道，9,342,464 个有效帧，74,739,712 字节音频数据，记录丢帧为 0；停止后完成频谱分析并保存。该段末尾包含手动停止前的下一首歌曲，保留为未绑定的混合片段，只作为长时间采集证据，不作为纯单曲或自动整曲分段验收。
- 使用前述已核对的 8.8 秒 `Lean On` 录音，在正式应用中读取已有测试歌单、确认录音归属，再以拉斐尔曲线和平直计算基线完成匹配。UI 显示 D 约 2.9 dB、Dhigh 约 3.5 dB，10–20 kHz 相对参考约 -2.1 dB、该频段能量占比约 0.73%；这些是该片段的结果，不是整曲结论或音质打分。
- 正式应用从该匹配结果新建单曲测试歌单，正常网页回读确认歌曲 ID `30854130`、数量 1 和顺序，receipt 状态 `verified`。本次只新增 1 个测试歌单，原有歌单未改动。由此通过手动采集、分析、身份绑定、匹配、创建和回读流程；没有使用独立写入 probe 代替此次生产 UI。
- 辅助功能权限尚未开启，因此 AX 驱动的自动识别、连续切歌分段和自动采集全链路仍未验收。

## 0.2.1 纠偏验证

- 恢复网易云新建歌单、预览选择、逐首添加和目标回读；保留轻量创建凭据，未知结果不重复创建。静态核验覆盖同 request 不重放、完成后新 request 可创建、目标 ID 与完整曲目顺序核对，以及创建时已自动加入首曲的处理。JS 语法与创建点击 fixture 通过；尚未完成真实登录账号的写入验收。
- 修正频段相对增益未减全局电平的问题。真实拉斐尔录音的 D/Dhigh/C 保持下表数值；10–20 kHz 三段相对增益为约 +0.52 / -1.35 / -5.22 dB，能量加权平均为 -1.77 dB。界面已实测显示 -1.8 dB，不再显示原始测量电平 +109 dB。
- `verify-core.sh` 新增的曲线/参考整体 ±120 dB 平移不变断言通过，覆盖 C、D、Dhigh 和频段相对增益。低能量 10–16 kHz 估计通过；没有数据时仍不填数值。
- 精确歌曲 ID 可选择已有分析录音，本次播放仍另外积累；`verify-listening.sh` 21 项通过。跟随播放的授权后实机联动仍待验收。
- Release 构建、签名验证通过。0.2.1 可执行文件 SHA-256：`96d1371bb24e38d2ebdc4327b8df7444bff8586e822141e2042254dbc70f573a`。

本轮真实数值证据另存 `test-output/verification/real-raphael-result-0.2.1.txt`，保留 0.2.0 历史结果。

## 已跑通的真实数据链

使用已保存在本机的真实播放器录音（27.648 秒、48 kHz、双声道），重新分析全部 PCM，得到 645 个频谱帧。耳机为仓库内 HBB 拉斐尔右声道 480 点 sample，参考为明确标注的 0 dB 平直计算基线。

| 结果 | 实测值 |
| --- | --- |
| 参考偏差 D | 3.1776 dB |
| 10–20 kHz 偏差 Dhigh | 3.7419 dB |
| 谱形变化 C | 0.071777 |
| 歌曲 10–20 kHz 能量占比 | 0.07237% |
| 实际计算范围 | 20 Hz–20 kHz |

录音有历史丢帧记录，验证明确使用 partial + gaps + 未确认身份输入。结果正常返回估计值，并说明已采时长与缺口；没有把缺口补成静音，没有把片段认定为完整歌曲。平直参考不代表理想听感，以上数字仍需要用户试听判断用途。

本机证据为 `test-output/verification/real-raphael-result.txt`。录音、频谱和本机诊断日志不随公开仓库分发。

## Process Tap 启动与落盘

旧配置启用了 `kAudioAggregateDeviceTapAutoStartKey=1`。本机 Apple SDK 的 `AudioHardware.h` 注释明确说明，这会让 `AudioDeviceStart` 等待 tap 首次收到音频；与旧阻塞样本一致。0.2.0 移除此配置，保留后台启动、取消清理与 SPSC 缓冲。

- 独立真实设备诊断：合成播放进程连续三次 start 为 61/64/67 ms，并收到非零音频；空闲网易云进程为 45 ms，收到静音回调。
- 生产调用链 `AudioCapture.start → IOProc → ring → CAF → stop` 连续多次完成。精简后的 `zsh scripts/verify-process-tap.sh` 最后一次为 96,256 帧、188 callbacks、0 dropped，CAF 774,144 bytes，非零音频检查通过。
- 该脚本播放合成音测试真实硬件路径；这不是网易云整曲自动采集验收。

## 自动验证与构建

- `scripts/verify-core.sh`：真实曲线导入、log-Hz 插值、不外推、声道保留、PSD、D/Dhigh/C、频率单元积分、扩展频段通过；新增任意参考、partial/unknown/gaps 可计算、相同 PSD 的缺口不改变 D、非有限数据拒绝断言通过。
- 同一核心脚本的 60 秒音频包含前、中、后三段不同频率，分析到最后一秒，三段均贡献能量，没有 30 秒截断。
- `scripts/verify-listening.sh`：21 项关键行为通过，包括默认开启、关闭持久化、重复轮询、暂停/切歌、跳播、显式重试和禁用后不重启。
- `zsh scripts/verify-capture.sh`：20 万帧顺序一致、零丢帧，受控溢出计数 4096 正确。
- `zsh scripts/verify-capture-cancellation.sh`：取消/完成先后与超时竞态通过。
- XCTest 在当前 Command Line Tools 环境缺少模块，使用上述原生 Swift probes；不将未运行的 XCTest 描述为通过。

此前 0.2.0 的 Release 与硬件验证记录对应可执行文件 SHA-256：`16ea98d97c6981f8af495c648e53375dafb87a0bf7fb54f1dfe7e9c8013f55b2`。0.2.1 的音频采集底层未改动，数值验证与构建已重新执行。

## 0.2.1 历史界面验证记录

- 本轮 Mac 已解锁，已在最终 0.2.1 应用中验证真实录音结果、修正后的高频说明、两个模式的参考/频段组头，以及“生成网易云歌单”预览和内嵌登录网页。
- 0.2.1 验证时内嵌网易云页面显示未登录；本机唯一已分析录音也未绑定网易云歌曲 ID。预览正确显示待绑定说明，创建按钮禁用。没有替用户猜歌曲绑定，也没有向账号创建测试歌单。
- 尚未完成辅助功能授权后的网易云身份识别、连续整曲采集与切歌联动端到端验收。采集始终显示实际已录时长，播放器观察不充当精确音频边界。
- 0.1.1 曾实测自动积累默认开启、关闭后重启仍关闭；本轮保留同一偏好键，并以策略 probe 回归。
- 既往网页读取曾取得毁 HiFi 957 个真实频响点及网易云公开歌单 10 条 ID/标题/顺序；本轮未改动两个读取脚本。
- 0.2.1 恢复网易云写入功能，本地 JSON 导出保留；实际账号创建成功仍需单独验证。

应用使用本机临时签名，未公证。原始用户资料和历史导出 journal 保留在本地，本轮未清除。
