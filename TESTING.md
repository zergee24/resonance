# 本机验证记录

日期：2026-09-24。macOS 26.6.2（25G83），Apple Silicon，Swift 6.3.3，部署目标 macOS 14.2。

本页记录开发机上的实际观察；下文引用的 `test-output/` 日志、原始文件和录音仅保存在本机，不随公开仓库分发。公开仓库附带从真实拉斐尔文件提取的 480 点数值 sample（`Samples/Raphael/`），去除了设备标识等导出元数据。`scripts/verify-core.sh` 默认验证该 sample，并用独立格式样例检查 AudioTools 元数据过滤；重新运行会生成当前环境的报告。

## 0.1.1 自动积累更新

- 真机界面：首次打开“自动积累听歌资料”为开启；点击关闭，正常退出进程后重启仍为关闭，且显示“未开始监听”。随后手动恢复开启，显示“需要辅助功能权限；尚未开始记录”。测试未授予新的系统权限，资料库未增加伪造的歌曲或音频。
- `scripts/verify-listening.sh`：71 项策略断言通过，覆盖默认开启、关闭持久化、切歌、暂停后恢复、重复轮询、跳播、新连续段与显式重试；同段重试不重复创建历史条目。
- `scripts/verify-capture-cancellation.sh`：取消先于完成、完成先于取消、重复取消、timeout 竞态通过。使用受控启动状态机验证原队列清理顺序，不代表真实 Core Audio 启动问题已经解决。
- `scripts/verify-audio-window.sh`：60 秒、48 kHz 双声道音频的 2,880,000 帧完整保留，末段内容未截断；边界裁剪保持源文件不变，非法范围被拒绝。
- `scripts/verify-core.sh`：原有声学断言及新增长时间轴断言通过。60 秒音频前、中、后各使用不同频率，分析覆盖到最后一秒，三部分均进入能量特征，没有固定 30 秒截取。
- `zsh scripts/verify-capture.sh`：20 万帧顺序、零丢帧及预期溢出计数通过。
- 最终 Release 构建与 `codesign --verify --deep --strict` 通过。0.1.1 可执行文件 SHA-256：`37e53f7d14a5f50b76197960b9d153a21552656ce375fbb1a8af2c4abb054943`。

真实网易云自动识别、持续采集到整曲分析尚未端到端验收：本机尚缺共鸣的辅助功能授权，原有 Core Audio 启动超时也没有被证明解决。切歌/跳播的精确音频边界不能由 UI 轮询证明，此类段即使保守截尾并保存频谱，也不参与自动排名。只有实际覆盖与身份等证据满足条件的录音才标为整曲；不同播放段当前分别留存，不自动拼成整曲。

## 0.1.0 已直接观察

| 路径 | 真实结果 |
| --- | --- |
| 原生应用构建、启动 | SwiftUI 两模式界面、SQLite 资料库及 WKWebView 正常启动；空库不生成假曲线或假推荐 |
| 毁 HiFi 查询 | 拉斐尔搜索当前显示“暂无数据” |
| 毁 HiFi 测评曲线读取 | 测评 `364618c9-eff1-4529-9c82-98d9d4d8ddb3` 的软耳 EDGE 页读到 957 个 FR 点；表单显示 20–19897 Hz，来源 URL 保留；未把测试耳机标记为用户拥有 |
| 网易云公开歌单读取 | `playlist?id=509265479` 读取 10 首精确歌曲 ID、标题及顺序；页面标称 111 首，应用显示“未证明完整”；没有写入账号 |
| 网易云进程采集（第一次） | 修复 CATapDescription 初始化后，真实播放 Don't Be So Serious / Low Roar，采到 27.648 秒、48000 Hz、双声道 PCM，完成频谱分析并持久化；回读发现丢帧 3584（约 74.7 ms），因此未参与排名 |
| 歌单录音绑定表单 | 显示精确歌曲 ID、原始顺序及录音“限制比较”状态；未选择目标/未确认版本时提交不可用。测试取消，没有错绑数据 |
| 当前播放身份 | 原生界面 AX 可用信息有限；应用显示需要辅助功能权限，尚未完成权限后的端到端识别验证。实录保留为“未识别片段”，未伪造歌曲 ID |
| 文件解析 | 原生验证程序直接解析 HBB 拉斐尔 AudioTools 文件：480 点，19.5–20000 Hz，尾部 metadata 不被当成频响 |

录音未做播放处理声明且存在丢帧，故该实录没有进入通用耳机排名。频谱分析成功与声学推荐成立分别记录。

第二次尝试在已经播放时启动录音，发现 Core Audio `AudioDeviceStart` 同步调用阻塞主线程。采样证据保存于 `test-output/verification/audio-start-hang-sample.txt`；已结束本次自建测试进程，未重启系统音频服务。

最终修复将 Core Audio 启动移至串行后台队列，10 秒超时只结束 UI 等待，晚返回由同队列清理，避免销毁与启动竞态。最终构建真机再次出现系统调用超时，已验证错误会显示且界面仍可操作；启动超时的底层原因尚未排除，**连续录音稳定性验收未通过**。没有宣称系统服务、驱动或权限就是已确认根因。

录音缓冲由 trylock 改为预分配单生产者/单消费者原子游标：20 万帧并发读写顺序完整、零丢帧；满载测试预期与实际均报告丢弃 4096 帧。可通过 `zsh scripts/verify-capture.sh` 重现。这证明缓冲实现，不代替仍未通过的设备级连续录音验收。

## 数值核验

`scripts/verify-core.sh` 已执行通过，结果见 `test-output/verification/core-harness.md`。覆盖 log-Hz 插值、不外推、非有限/冲突点拒绝、Hann PSD 双侧峰值、平坦参考 C/D=0、D_high 内容阈值、缺参考/数据保护、FFT cell 重叠积分、20–25 kHz 实际只至 24 kHz、原采样率 CAF 双声道解码。

`swift test` 受当前 Command Line Tools 缺少 XCTest 阻塞，未将源码存在或编译通过描述为 XCTest 通过。

## 尚未验收

- 新授权后的当前歌曲识别及切歌/跳播联动实际路径。
- 真实账号新建网易云歌单、权限/登录失效、部分写入以及完整写后回读。已实现预览确认、账号核对、持久化状态与不确定时停止；不能据此宣称外部写入已经成功。
- 拉斐尔与第二副同测量体系耳机的实际排名：尚缺已核验兼容参考与用户拥有的可比耳机数据。
- 整曲连续采集、长歌单批量采集与自动恢复；首版目前使用逐首人工操作。
- 文件选择器 UI 导入流程未完成最终真机验收；直接数值解析与网页导入表单已分别核验。

当前测试数据在应用本地资料库中：10 个公开歌单待采集条目和一条真实录音。没有向账号创建、修改或删除歌单。

最终 Release 应用已构建并启动，`codesign --verify --deep --strict` 通过。可执行文件 SHA-256：`5cb31aadda81a58850dae294916799b700c1612a2a180710e80df289a5efc376`。应用仅本机临时签名，未公证。
