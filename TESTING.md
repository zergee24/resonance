# 本机验证记录 · 0.2.6

日期：2026-09-25—2026-09-26。macOS 26.6.2，Apple Silicon，Swift 6.3.3，部署目标 macOS 14.2。各版本证据分列如下。

## 0.2.6 系统播放器接入

- 复用 dsh 插件的 MediaRemoteMini 只读桥接。正式应用读取完整歌名、艺人、专辑、进度、来源 Bundle ID / PID；系统路径无须辅助功能或窗口 OCR，旧系统仍可用备选。二进制与 BSD-3-Clause 许可证随包分发。
- `verify-system-player.sh` 通过解码、空状态、可选进度、来源、超时与输出上限检查，以及本机暂停网易云读取。`verify-player-ocr.sh` 通过来源隔离、不透明 UUID 不冒充歌曲 ID、系统优先不被 OCR 覆盖、旧 JSON 兼容及原 OCR 检查。自动积累策略 21 项检查通过。
- 系统元数据与下列两段录音初测使用 0.2.6 build 9，SHA-256 `cbe97f3fda4ca108e5e792aad95cc6095c2da0ceb82eccfd55d0cb2bec0a28d5`。EOF 修复后的最终同版 Release SHA-256 为 `31cc22bd8480e5f09f8883dc089c98ff8e85d653dc0c4522a7c271209e5b2e2e`；构建、外层 App 与内嵌 dylib 签名检查通过。
- 正式 UI 读到 `With an Orchid / Yanni`、`1:04 / 5:07`、网易云音乐、`com.netease.163music`、PID 896。切到旧 OCR 失败的歌曲后，完整显示 `A Moment Apart (Live) (ODESZA VIP Remix) / ODESZA`、6:23；执行网易云最小化后进度与录音继续增长，未开启窗口识别。
- 更新临时签名后首次录音启动超时；通过系统设置更新共鸣原有 AudioCapture 授权并重开应用后恢复。本轮未重启 coreaudiod，未改原有歌单。
- 系统驱动切歌与暂停分别保存 50.517333 秒长标题段与 35.008 秒 `With an Orchid`；均为 48 kHz 双声道 Float32、零丢帧，分别为 2,424,832 / 1,680,384 帧，有限非零 PCM。完整标题、艺人、来源应用、Bundle ID、实际采集 PID、metadataSource=systemPlayer、媒体起点均随记录保存；未写虚假的网易云 ID。
- 35.008 秒段频谱已成功写入。50.517333 秒段首次分析失败已定位：2,424,832 帧恰好等于 37 × 65,536，解码循环在读完最后一个完整块后额外调用 AVAudioFile.read，触发 Foundation._GenericObjCError。独立分块探针确认前 37 块均有效，错误发生于额外的 EOF 读取；未进入频谱压缩或写入。修复为按剩余帧数读取后，同一真实 CAF 成功生成 1,181 个频谱帧和约 133 MB 的压缩特征文件。
- 修复后的独立生产解码 probe 通过 131,072 帧整数块和 131,195 帧非整数块 CAF；`verify-core.sh` 全部检查通过，覆盖完整时间线、双声道、PSD 与 C/D/Dhigh。新增 XCTest 用例已保存，但本机缺少 XCTest 模块，`swift test` 未运行成功；没有将独立 probe 冒充 XCTest 通过。
- 失败的 50.5 秒段使用修复后的生产分析与存储代码重新生成特征，并恢复同一资料库记录；原音频、歌名、来源、PID 和录音起点不变。最终签名应用再次完成系统识别、自动录音、暂停保存与分析：`With an Orchid` 27.136 秒、48 kHz 双声道、1,302,528 帧、零丢帧。最终退出测试播放，保留自动积累开启。
- 私有服务依赖实际运行的 macOS 版本；本机验证为 macOS 26.6.2，没有在 macOS 14.2 真机重新验证 AX/OCR。播放器轮询依然不证明精确整曲边界，录音保留为片段。本机原始证据见忽略目录 `test-output/verification/live-recording-0.2.6.json`。35 秒段正式 UI 匹配拉斐尔得到 D 3.1 dB、Dhigh 2.9 dB；只代表该片段和平直参考。

## 0.2.5 图标与最终真机验证

- 正式应用增加耳机与频谱曲线图标，Dock 图标包含 16–1024 像素标准尺寸，应用侧栏已实测显示；原图与生成说明保存在 `Support/AppIcon.png`、`Support/AppIcon.md`。
- 本轮真实播放发现红色 VIP 徽章被 OCR 拼入歌名，随后徽章识别变化会误切段。现在只在底栏元数据区屏蔽饱和红色徽章像素；不删除歌名里的 VIP 字符，不改变白色/灰色标题。生产像素检查验证红色过滤、白色标题保留、图像方向，以及原有两次确认/暂停/过期处理。
- 最终 Release 为 0.2.5 build 8，可执行文件 SHA-256：`c06526c8f15d312131dd02928b6d35d1bae8edb74361b717453541476f1f2d4f`。构建、签名、OCR probe 与 21 项自动积累策略检查通过。
- 最终签名版本真实播放 `Lean On`：从开头连续录到曲尾，单段 175.050667 秒、48 kHz 双声道 Float32、8,402,432 帧，零丢帧；全段未因 VIP 徽章误切段，频谱文件保存成功。播放器没有提供完整时钟证据，因此资料仍保留“完整度未知”，不把 175 秒自动认定为精确整曲边界。
- 切至短标题 `With an Orchid` 后自动开始新段；网易云暂停即自动保存，64.458667 秒、3,094,016 帧、48 kHz 双声道、零丢帧。两段 PCM 均验证为有限且非零，RMS 分别约 0.134605 / 0.063737，录音与频谱均存在于本机资料库。
- 正式 UI 对第二段与拉斐尔计算得到 D 约 3.2 dB、Dhigh 约 2.9 dB；10–20 kHz 相对参考约 +0.2 dB，能量占比约 0.03%。这些数值只对应已采 64.5 秒和平直参考。
- 仍有限制：自然切到横向滚动的 `Moment Apart (Live)…` 时，OCR 无法连续确认稳定歌名，旧身份过期后停止录音；切至短标题后恢复。不能把这次结果写成任意长标题的连续整曲自动采集已通过。该场景可关闭自动积累并使用手动采集，切歌前停止。进程采样排除了主线程被频谱压缩阻塞；压缩实际运行在 utility 队列。
- 本轮没有重启 coreaudiod，没有修改原有网易云歌单；测试结束时网易云暂停、自动积累和窗口识别保持开启。音频采集底层及匹配内核未改动。本机原始证据保存在忽略目录 `test-output/verification/live-recording-0.2.5.json`，不随公开仓库分发。

## 0.2.4 构建与针对性验证

- 新增 OCR 候选连续两次一致后再切段的处理，过滤单帧歌名跳变；暂停/停止立即生效。候选尚未确认时，不会把暂停的旧歌曲重新开始录音，也不延长旧身份的新鲜度。真实切歌会增加约一次 OCR 读取的确认延迟，旧段仍标明可能含切换尾音。
- `zsh scripts/verify-player-ocr.sh` 通过：生产图像裁剪，以及单帧噪声、两次确认、暂停、旧身份过期、重新恢复、空标题和首次 OCR 中夹有空辅助功能轮询的场景。`zsh scripts/verify-listening.sh` 的 21 项策略检查通过。
- 0.2.4 build 7 Release 构建与签名检查通过，可执行文件 SHA-256：`a7c2ea1c05cac5f30a05040e6d9cbf8acc192acdc3dc0f414aa520e561a167f4`。音频采集底层未修改。
- 该版本未单独完成最终真机验收，相关修复已纳入上方 0.2.5 继续验证；以下录音恢复证据属于 0.2.3。

## 2026-09-26 中午：0.2.3 自动采集恢复

- 继续测试相同 0.2.3 二进制（SHA-256 见下一节），没有重启 coreaudiod、更换输出设备或重置权限。关闭自动积累和窗口识别后，手动采集立即成功；保存 43.36 秒、48 kHz 双声道录音，丢帧为 0，频谱分析与拉斐尔匹配完成。该段保留为未绑定片段。
- 随后重新开启窗口识别和自动积累，`Bones` 自动录音成功；网易云暂停后自动保存并分析。CAF 为 48 kHz、双声道 Float32，共 1,947,136 个有效帧、40.565333 秒、15,577,088 字节音频数据，丢帧为 0。UI 显示拉斐尔 D 约 3.1 dB、Dhigh 约 3.2 dB；只代表该片段。
- 退出并重新打开共鸣后，自动采集仍能启动；观察到真实切到 `Trouble I'm In` 时新建录音、暂停后保存分析。没有修改原有网易云歌单。
- 同时发现 OCR 候选偶尔在 `Bones`、`Bones VIP` 和误识别文本间跳变，同一首歌被误切成多段。录音服务恢复不等于识别稳定性通过，后续修正与验证分开记录。
- 当前结论是 IOProc 注册阻塞已解除、自动录音已恢复。关闭/开启窗口识别与恢复存在时序关系；重新开启识别后录音仍正常，因此没有把 ScreenCaptureKit 判定为根因，也没有为这个推测修改 Core Audio 实现。

## 2026-09-26 上午历史现场：0.2.3 窗口识别修复，自动采集当时超时

- 被测生产应用为 0.2.3 build 6。可执行文件 SHA-256：`dfc80b78ed9b0acf6e3d6f21cf2f95d23afa7f173ec3fe397234e365f3670435`。本节真机验证使用同一二进制，未修改音频采集或 DSP 内核。
- 修复 ScreenCaptureKit 截图裁剪方向：原来取到窗口顶部搜索框，改为底部 80 点播放栏，并保留 Retina 像素比例。歌名/艺人取左侧对齐文本，排除封面、收藏数和音质按钮；增加日文识别。
- 真实网易云生产 UI 核对通过：`銀の龍の背に乗って` / `中島みゆき`；切到 `Lean On` 后歌名正确，艺人为可见的 `Major Lazer / DJ Snake /...`。后者本来就在播放栏中截断，没有把不可见的剩余艺人补成精确身份。
- 只读网易云“控制”菜单取得播放/暂停状态；暂停时共鸣显示暂停，继续播放时触发自动采集尝试。应用没有为状态读取打开或点击菜单。窗口识别的显式开启选择在多次重启后恢复，手动关闭自动积累后再开启也已实测。
- 本机旧临时签名的 TCC 授权与新二进制不匹配，曾出现系统开关已开启但应用无权限。针对共鸣单独更新辅助功能、录屏及系统录音授权；录屏与辅助功能已通过上述实际读取证明有效，没有重置其他应用权限。
- 自动采集未通过：更新系统录音授权后仍出现 `Audio capture setup timed out while Core Audio was starting.`。启动期间对生产进程采样，setup queue 阻塞在 `CaptureSession.start → AudioDeviceCreateIOProcID → HALC_ProxyIOContext::_TellServerAboutStreamUsage → HALC_ProxyObject::SetPropertyData → mach_msg`。系统已注册 tap/aggregate，但尚未进入 `AudioDeviceStart`；不能把本次问题写成录音成功或单纯权限未开。
- 本次没有产生可供分析的新 PCM，也未完成自动切歌分段或整曲采集验收。失败尝试保留为待采集元数据，未使用历史录音冒充本次录音；没有新建或修改网易云歌单。测试结束时网易云暂停、自动积累和窗口识别保持开启。
- Release 构建、临时签名校验通过。`zsh scripts/verify-player-ocr.sh` 调用生产裁剪函数，验证普通图像底部 80 像素、2x Retina 底部 160 像素及小图像边界；像素检查证明不会混入顶部区域。未改动的 DSP 历史验证保留如下，不标为本版本重新执行。

## 2026-09-25：0.2.2 真实账号测试

被测代码提交：`79db5a7`。

- 使用真实已登录 WebView 和生产 `WebWorkspace` / `PlaylistWriter` 源码编译独立 UI probe，隔离尚未成功的音频采集前置。没有用假录音或猜测歌曲 ID 进入声学匹配；这次证明的是网页写入链路，不是录音到歌单全链路。
- 单曲创建与同请求只读恢复通过。最终双曲请求自动完成创建、逐首添加、目标回读，期望和实读顺序均为 `30854130 → 2083530698`，状态 `verified`；再次执行同请求仍为两首，没有重复创建或加歌。
- 真机修正：隐藏账号菜单中的 `/login` 不再误判为未登录；收藏到歌单使用 `fav` 而不是播放队列 `addto`；歌单使用普通文档 URL，避免 hash 跳转不触发 WebKit 加载回调；等待异步列表出现目标 ID；按反序添加以抵消网易云的首部插入。
- 歌单解析限定主内容，修正侧栏“500首歌曲”污染当前三首歌单总数，以及“我的音乐”页面标题误抓侧栏。真实三首歌单回读名称正确、总数 3、`complete`。
- 只新增 4 个明确标注的测试歌单，保留前面失败的测试现场。原有歌单未选作写入目标，并做了只读内容与数量核对。账号 ID、用户歌单明细、录音和本地 receipt 不提交到仓库。
- `node Tests/WebProbe/playlist-writer.test.cjs` 通过，覆盖隐藏登录项、真实可见登录、未知状态、精确歌曲 ID 与收藏动作。
- 0.2.2 Release 构建、签名验证与正式应用启动通过。可执行文件 SHA-256：`fa3f9c6b905f3918e76d03439c21c8c71b8596637ffc769dccf49e89ea795cc4`。

### 2026-09-25 历史失败记录：真实音频采集当时未通过

两次 GUI 实测均在 `AudioDeviceStart` 超时；进程采样和 Core Audio 日志显示 HAL server `0x10004003`，失败 CAF 只有文件头、0 音频帧。退出共鸣后，独立生产采集 probe 对同一网易云进程也失败；加入默认输出 clock/subdevice 的诊断对照没有恢复。没有凭此修改采集底层或增加备用框架，也未擅自重启系统音频服务。

0.2.0 的 tapautostart 修正使当时的独立 probe 通过，但不能证明当前真实客户端录音已修好。该结论只适用于当时未重启音频服务的现场；当前可导入已有本地音频进行匹配。新增生产 probe 的 `--pid` 参数，便于直接复测指定播放进程。

### 2026-09-26 重启 coreaudiod 后：生产音频采集通过

- 在录音权限已开启的前提下，重启本机 `coreaudiod` 后重新启动同一 0.2.2 生产应用；未修改采集代码、默认音频设备或隐私权限。该次验证确认恢复条件包含干净的系统音频服务状态，不能把“权限已开启”单独视为充分条件。
- 生产 GUI 触发 `AudioCapture.start → process tap/aggregate → IOProc → ring → CAF → stop` 成功。CAF 为 2 声道、48 kHz、Float32 交错格式，时长 8.8 秒，422,400 个有效帧，3,379,200 字节音频数据，文件大小 3,383,296 字节；无 `0x10004003` 或 `StartIO` 错误。
- coreaudiod 日志确认 tap IOContext 注册、客户端进入 `running: yes`，停止时正常注销并释放防休眠 assertion。0.2.2 可执行文件 SHA-256 仍为 `fa3f9c6b905f3918e76d03439c21c8c71b8596637ffc769dccf49e89ea795cc4`。
- 随后连续录音 194.634667 秒，48 kHz 双声道，9,342,464 个有效帧，74,739,712 字节音频数据，记录丢帧为 0；停止后完成频谱分析并保存。该段末尾包含手动停止前的下一首歌曲，保留为未绑定的混合片段，只作为长时间采集证据，不作为纯单曲或自动整曲分段验收。
- 使用前述已核对的 8.8 秒 `Lean On` 录音，在正式应用中读取已有测试歌单、确认录音归属，再以拉斐尔曲线和平直计算基线完成匹配。UI 显示 D 约 2.9 dB、Dhigh 约 3.5 dB，10–20 kHz 相对参考约 -2.1 dB、该频段能量占比约 0.73%；这些是该片段的结果，不是整曲结论或音质打分。
- 正式应用从该匹配结果新建单曲测试歌单，正常网页回读确认歌曲 ID `30854130`、数量 1 和顺序，receipt 状态 `verified`。本次只新增 1 个测试歌单，原有歌单未改动。由此通过手动采集、分析、身份绑定、匹配、创建和回读流程；没有使用独立写入 probe 代替此次生产 UI。
- 辅助功能权限尚未开启，因此 AX 驱动的自动识别、连续切歌分段和自动采集全链路仍未验收。

## 0.2.1 纠偏验证

- 恢复网易云新建歌单、预览选择、逐首添加和目标回读；保留轻量创建凭据，未知结果不重复创建。静态核验覆盖同 request 不重放、完成后新 request 可创建、目标 ID 与完整曲目顺序核对，以及创建时已自动加入首曲的处理。JS 语法与创建点击 fixture 通过；尚未完成真实登录账号的写入验收。
- 修正频段相对增益未减全局电平的问题。真实拉斐尔录音的 D/Dhigh/C 保持下表数值；10–20 kHz 三段相对增益为约 +0.52 / -1.35 / -5.22 dB，能量加权平均为 -1.77 dB。界面已实测显示 -1.8 dB，不再显示原始测量电平 +109 dB。
- `verify-core.sh` 新增的曲线/参考整体 ±120 dB 平移不变断言通过，覆盖 C、D、Dhigh 和频段相对增益。低能量 10–16 kHz 估计通过；没有数据时仍不填数值。
- 精确歌曲 ID 可选择已有分析录音，本次播放仍另外积累；`verify-listening.sh` 21 项通过。跟随播放的授权后实机联动仍待验收。
- Release 构建、签名验证通过。0.2.1 可执行文件 SHA-256：`96d1371bb24e38d2ebdc4327b8df7444bff8586e822141e2042254dbc70f573a`。

本轮真实数值证据另存 `test-output/verification/real-raphael-result-0.2.1.txt`，保留 0.2.0 历史结果。

## 已跑通的真实数据链

使用已保存在本机的真实播放器录音（27.648 秒、48 kHz、双声道），重新分析全部 PCM，得到 645 个频谱帧。耳机为仓库内 HBB 拉斐尔右声道 480 点 sample，参考为明确标注的 0 dB 平直计算基线。

| 结果 | 实测值 |
| --- | --- |
| 参考偏差 D | 3.1776 dB |
| 10–20 kHz 偏差 Dhigh | 3.7419 dB |
| 谱形变化 C | 0.071777 |
| 歌曲 10–20 kHz 能量占比 | 0.07237% |
| 实际计算范围 | 20 Hz–20 kHz |

录音有历史丢帧记录，验证明确使用 partial + gaps + 未确认身份输入。结果正常返回估计值，并说明已采时长与缺口；没有把缺口补成静音，没有把片段认定为完整歌曲。平直参考不代表理想听感，以上数字仍需要用户试听判断用途。

本机证据为 `test-output/verification/real-raphael-result.txt`。录音、频谱和本机诊断日志不随公开仓库分发。

## Process Tap 启动与落盘

旧配置启用了 `kAudioAggregateDeviceTapAutoStartKey=1`。本机 Apple SDK 的 `AudioHardware.h` 注释明确说明，这会让 `AudioDeviceStart` 等待 tap 首次收到音频；与旧阻塞样本一致。0.2.0 移除此配置，保留后台启动、取消清理与 SPSC 缓冲。

- 独立真实设备诊断：合成播放进程连续三次 start 为 61/64/67 ms，并收到非零音频；空闲网易云进程为 45 ms，收到静音回调。
- 生产调用链 `AudioCapture.start → IOProc → ring → CAF → stop` 连续多次完成。精简后的 `zsh scripts/verify-process-tap.sh` 最后一次为 96,256 帧、188 callbacks、0 dropped，CAF 774,144 bytes，非零音频检查通过。
- 该脚本播放合成音测试真实硬件路径；这不是网易云整曲自动采集验收。

## 自动验证与构建

- `scripts/verify-core.sh`：真实曲线导入、log-Hz 插值、不外推、声道保留、PSD、D/Dhigh/C、频率单元积分、扩展频段通过；新增任意参考、partial/unknown/gaps 可计算、相同 PSD 的缺口不改变 D、非有限数据拒绝断言通过。
- 同一核心脚本的 60 秒音频包含前、中、后三段不同频率，分析到最后一秒，三段均贡献能量，没有 30 秒截断。
- `scripts/verify-listening.sh`：21 项关键行为通过，包括默认开启、关闭持久化、重复轮询、暂停/切歌、跳播、显式重试和禁用后不重启。
- `zsh scripts/verify-capture.sh`：20 万帧顺序一致、零丢帧，受控溢出计数 4096 正确。
- `zsh scripts/verify-capture-cancellation.sh`：取消/完成先后与超时竞态通过。
- XCTest 在当前 Command Line Tools 环境缺少模块，使用上述原生 Swift probes；不将未运行的 XCTest 描述为通过。

此前 0.2.0 的 Release 与硬件验证记录对应可执行文件 SHA-256：`16ea98d97c6981f8af495c648e53375dafb87a0bf7fb54f1dfe7e9c8013f55b2`。0.2.1 的音频采集底层未改动，数值验证与构建已重新执行。

## 0.2.1 历史界面验证记录

- 本轮 Mac 已解锁，已在最终 0.2.1 应用中验证真实录音结果、修正后的高频说明、两个模式的参考/频段组头，以及“生成网易云歌单”预览和内嵌登录网页。
- 0.2.1 验证时内嵌网易云页面显示未登录；本机唯一已分析录音也未绑定网易云歌曲 ID。预览正确显示待绑定说明，创建按钮禁用。没有替用户猜歌曲绑定，也没有向账号创建测试歌单。
- 尚未完成辅助功能授权后的网易云身份识别、连续整曲采集与切歌联动端到端验收。采集始终显示实际已录时长，播放器观察不充当精确音频边界。
- 0.1.1 曾实测自动积累默认开启、关闭后重启仍关闭；本轮保留同一偏好键，并以策略 probe 回归。
- 既往网页读取曾取得毁 HiFi 957 个真实频响点及网易云公开歌单 10 条 ID/标题/顺序；本轮未改动两个读取脚本。
- 0.2.1 恢复网易云写入功能，本地 JSON 导出保留；实际账号创建成功仍需单独验证。

应用使用本机临时签名，未公证。原始用户资料和历史导出 journal 保留在本地，本轮未清除。
