# 共鸣 · AI 歌单使用说明

版本：0.1.1

## 安装与启动

支持 macOS 14.2+、Apple Silicon。打开 DMG，将“共鸣”拖入 Applications 后启动。

本机临时签名、未公证。遇 macOS 安全提示，只用系统审核或打开入口决定是否继续；不以命令绕过或降低安全设置。无需模型 Token、API Key、Python、Codex。

## 第一次设置

先在“我的声学资料库”导入耳机频响 CSV/TXT，或从“查询耳机曲线”的普通网页读取曲线。只使用已确认的频率响应数值，填写实际测量体系和范围；确实拥有的耳机勾选“我拥有”。

再导入同一测量体系、同一补偿基准下的参考目标并关联耳机。体系未知、参考缺失、未覆盖 20 Hz–20 kHz 或实际不可比时不排名；不能只改名称制造兼容关系。

## 导入歌曲与录音

可导入本地音频，或播放网易云使用“自动积累听歌资料”。开关首次默认开启；手动关闭会保留，下次不强开。它独立于“跟随当前播放”：仅在监听到 `playing` 且有歌曲 ID 或候选标题时记录信息并尝试采集，不补 ID。权限不足仅提示并记录可读元数据；首次系统音频录音权限由用户处理。

连续采集实际播放内容，无固定 30 秒上限；按“停止并分析”或暂停、关闭、退出结束并分析当前段。“停止并分析”只结束当前段，下一次切歌或暂停后再播才恢复；失败可重试当前歌曲，不会无限重试。缺少首尾或跳过只能标为片段；播放段分别留存，不跨母带/处理合并，整曲排名只用完整证据。自动积累不会主动切歌、批量播放或写入网易云歌单；界面显示状态并可随时关闭。采集播放器进程输出，不是麦克风；采集前确认共同播放设置、EQ 与空间处理声明。

生成子歌单前，在资料库点击录音“用于歌单曲目”，选择歌单原始行并确认歌曲版本。相同平台 ID 也不能证明母带或播放处理相同；本地耳机匹配可直接使用已导入音频。

当前歌曲识别需辅助功能权限，窗口识别备选另需屏幕录制权限；授权前可能只有“未识别片段”。片段要参与“根据耳机找歌曲”需打开“允许片段结果”。没有自动批量播放/采集队列；可逐首采集，或批量导入后逐条绑定歌单行。

## 两种使用模式

“根据歌曲找耳机”比较“我拥有”的耳机；“根据耳机找歌曲”按谱形变化或参考偏差排序。C 表示谱形变化，D 越低越接近参考；数值不等于音质，不能替代听音。

应用可读取普通网页中可见的网易云歌单；页面不完整时只保留已读歌曲。内置网页与客户端登录状态独立。结果可先预览并核对 ID、顺序；新建歌单及写入后的完整回读尚未真实验收，预览不代表成功，不确定时不要重复创建。

## 当前限制与本地数据

Core Audio 启动仍可能超时，原因未解决；应用约 10 秒内报错并保持可操作，录音稳定性未验收。切歌、跳播或身份丢失时，按最后一次同曲身份观察时间保守排除尾部，保留原 PCM/分析物；轮询不能证明交叉淡入边界，该段不自动排名。暂停或手动停止仅在身份相同、无丢帧且处理已声明等证据齐全时按实际覆盖评估。

资料在 `~/Library/Application Support/Resonance`，含元数据、原音、频谱和网页日志。用界面停止；失败片段留本机但不排名。关闭不会写歌单。
