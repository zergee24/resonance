# 共鸣 · AI 歌单使用说明

## 安装与启动

本版本支持 macOS 14.2 或更高版本，当前构建面向 Apple Silicon。打开收到的 DMG，将“共鸣”拖入“Applications（应用程序）”，再从应用程序文件夹启动。

这是仅供本机使用的临时签名版本，尚未完成公证。首次打开时如果 macOS 提示来源或安全性问题，请使用系统提供的正规审核、允许或打开入口自行决定是否继续；不要通过命令绕过检查，也不要降低系统安全设置。运行应用不需要模型 Token、API Key、Python 或 Codex。

## 第一次设置

先在“我的声学资料库”导入耳机频响 CSV/TXT，或在“查询耳机曲线”中打开普通网页的测评详情并读取曲线。只使用已确认的频率响应数值，填写实际测量体系和有效范围。对确实属于自己的耳机，勾选“我拥有”。

源码仓库附带 `Samples/Raphael/Artipical_Raphael_HBB_R.csv`，可导入体验拉斐尔曲线。它是 HBB 的真实右声道测量，测量体系请保留为 `unknown`；未提供兼容参考，不会自动生成排名，也没有 20 kHz 以上数据。详情见同目录的来源说明。

随后导入同一测量体系、同一补偿基准下的兼容参考目标，并把参考关联到耳机。测量体系未知、参考缺失、频响没有覆盖 20 Hz–20 kHz，或两者并不真正可比时，应用不会给出排名；不能只改体系名称来制造兼容关系。

## 导入歌曲与录音

可以导入本地音频，也可以播放网易云音乐后点击“采集网易云音频”。播放后至少保留 15 秒非零音频，再按“停止并分析”结束。采集的是播放器进程输出，不是麦克风；PCM 和分析结果只保存在本机。采集前按实际情况确认界面中的共同播放设置、EQ 与空间处理声明，未确认时不生成通用耳机排名。

生成网易云子歌单前，在资料库点击录音的“用于歌单曲目”，选择导入歌单中的原始行，并确认歌曲与版本对应。平台歌曲 ID 相同，不能证明录音母带或播放处理相同。本地耳机匹配可以直接使用已导入音频。

当前歌曲识别需要系统的辅助功能权限，窗口识别备选另需屏幕录制权限；授权前应用可能只能保留“未识别片段”。在“根据耳机找歌曲”中使用片段，需打开“允许片段结果”。当前没有自动批量播放和采集队列，需要逐首采集，或批量导入本地音频后逐条绑定歌单原始行。

## 两种使用模式

“根据歌曲找耳机”用于比较已经标记为“我拥有”的耳机；“根据耳机找歌曲”用于按谱形变化或参考偏差给歌曲排序。C 表示谱形变化，D 越低表示越接近当前参考；这些数值不等于主观音质，也不能替代实际听音。

应用可以读取普通网页中可见的网易云歌单内容：打开歌单详情后点击“读取歌单”。网页显示不完整时只保留已读到的歌曲。内置网页与网易云客户端的登录状态独立，需要时在内置网页登录。歌单适配结果可先在应用内预览，检查歌曲 ID 和顺序；新建网易云歌单及写入后的完整回读目前都未完成真实账号验收，预览不代表外部写入成功。遇到不确定状态，请不要重复创建。

## 当前限制与本地数据

Core Audio 仍可能在启动采集时超时，底层原因尚未解决；应用会在约 10 秒内报告错误并保持界面可操作。录音稳定性尚未通过验收。采集失败或丢帧的录音不会进入有效排名，可优先使用本地音频导入。

资料保存在 `~/Library/Application Support/Resonance`，包括元数据、原始音频、频谱结果和网页写入日志。需要停止采集时使用界面的停止操作；失败片段可能仍保留在本机，但不会参与有效排名。关闭应用不会替你完成外部歌单写入。
