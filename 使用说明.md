# 共鸣 · AI 歌单使用说明

版本：0.3.1

默认正文 20 点、辅助信息与表格 18 点；窄窗口会将匹配结果与频段详情上下排列。

## 启动

打开 DMG，将“共鸣.app”拖到 Applications，再从“应用程序”启动。也可从源码运行 `zsh scripts/build-app.sh`，打开 `dist/共鸣.app`。支持 macOS 14.2+ 和 Apple Silicon；应用为临时签名、未公证版本，macOS 可能拦截首次启动，可在确认文件来源后通过系统设置“隐私与安全性”处理。不需要 Python、ffmpeg、API Key 或模型 Token。

图标中的耳机与双曲线对应“耳机频响 × 歌曲频谱”，已加入 Dock 和应用左上角。

## 先跑通一首歌

1. 打开“我的声学资料库”，点击“使用拉斐尔示例”。这会载入 HBB 真实右声道曲线（480 点，19.5 Hz–20 kHz）和“平直参考（计算基线）”0 dB 曲线。
2. 资料库预置 HE1 与 Alter Ego 无点档位，两条均来自毁 HiFi 的真实数值曲线。“我喜欢的声音”可多选、可关闭，关闭后会记住。喜欢的参考不会被当成你拥有的耳机。需要时也可导入自己的参考。
3. 用菜单“导入音频…”导入一首真实本地音频，或播放网易云并打开“自动积累听歌资料”。
4. 选择“根据歌曲找耳机”，选中歌曲，查看耳机的“偏好接近度”和更接近的参考；切到“根据耳机找歌曲”可按同一指标排序候选歌曲。

没有真实频谱或参考曲线时只显示原因，不会从歌名补出数值。

## 录音和资料

“自动积累听歌资料”首次默认开启。手动关闭后会记住，下次启动仍关闭；它只跟随正在播放的内容，不会主动切歌或批量播放。

播放器采集没有 30 秒上限。自动模式在能够识别播放状态时随暂停、切歌结束当前段；手动采集时，请在切歌前点击“停止并分析”，避免混入下一首。应用保留原始采样率、声道、格式和实际录音秒数；关闭或退出前会停止当前录音并保存已有内容。

每次采集都显示实际时长。无法证明从头到尾完整覆盖时，标为片段或完整度未知，不补造缺失时间。片段、小缺口、候选歌名和 EQ 未知仍可按已采内容估计，结果旁会给简短说明。

本地文件按完整文件导入。采集需要系统音频权限。macOS 15+ 优先从系统播放器读取完整歌名、艺人、专辑、播放进度和来源软件，这条识别路径不需要辅助功能或录屏权限，网易云窗口隐藏或最小化也能工作。当前采集仍只支持网易云；其他播放器的元数据可显示，但不会关联到网易云录音。系统未提供数据时才使用辅助功能备选，macOS 14.2 也使用备选识别。也可以点击“采集网易云音频”，手动“停止并分析”，再在资料库绑定对应歌曲链接。

系统播放器与辅助功能均未提供歌名时，可点击“启用窗口识别备选”，并在系统设置的“隐私与安全性 → 录屏与系统录音”中允许共鸣；首次授权后按系统提示退出并重新打开应用。它只读取网易云窗口，截图在内存中识别，不保存图片；歌名、艺人作为候选信息，网易云歌曲链接仍需绑定。这个开关会记住选择；系统播放信息有效时不进行窗口识别。使用 OCR 备选时保留网易云窗口可见；隐藏或最小化时可能无法识别，窗口中截断的艺人名也只能按可见内容读取。使用 OCR 时，横向滚动的长歌名也可能使自动识别不稳定；遇到“播放信息已过期”时，可关闭自动积累、手动点击“采集网易云音频”，在切歌前停止并分析。

本机临时签名的应用在更新后可能需要重新授权。若系统开关已开启，共鸣仍提示未授权，可从相应权限列表移除共鸣，再用“+”添加实际启动的那份“共鸣.app”；系统提示退出并重新打开时按提示操作。

如果系统录音权限已开启，重启共鸣后仍提示 Core Audio 启动超时，可在方便短暂中断声音时，在终端执行 `sudo killall coreaudiod`，输入 Mac 管理员密码，待声音恢复后重新启动共鸣。2026-09-26 本机曾通过这一操作恢复 0.2.2 的真实采集和分析；这不是每次使用的必要步骤，也不代表所有超时都有相同原因。

0.2.3 本机曾遇到 IOProc 注册超时，后来在同一 0.2.3 应用版本和系统音频服务中恢复，已完成自动录音、暂停保存与分析。遇到同类问题，可先关闭“自动积累”和“窗口识别”，重新启动共鸣，再开启窗口识别和自动积累；恢复后以实际录音秒数和分析结果为准。该操作是本机已走通的恢复路径，不代表已经查明所有超时的原因。0.2.6 保留正式图标，优先使用系统播放器元数据，避免窗口标题滚动和 VIP 徽章干扰；系统播放项 UUID 不等于网易云歌曲 ID，建单仍需绑定歌曲链接。当前实测结果见 [验证记录](TESTING.md)。

## 看懂结果

频谱分析完成后还会读取数据进行匹配；长录音需要等待底部计算指示结束，再看最终结果。

“根据歌曲找耳机”使用“我拥有”的耳机；“根据耳机找歌曲”固定一副耳机。默认使用“偏好接近度”，数值越小越接近所选个人参考集合中的一种声音。HE1 与 Alter Ego 分别计算整段结果，界面保留两者的数值，不逐段挑选或合成平均参考。

整体偏差使用听觉频带和压缩能量权重。局部 P90 是按压缩帧能量加权的误差 90% 分位值，减轻极低能量帧对这个统计值的影响；差异最大的位置是录音内时间，不一定是歌曲播放器上的位置，也不代表最容易听见的差异。10–20 kHz 另算偏差，PEQ 分段仍提供能量、带增益和覆盖状态。关闭全部个人参考后，使用耳机卡片中的单条参考。

也可切换旧 C、D、Dhigh 排序；排序切换使用已有结果，不重复解码音频。只有一副耳机也能工作。

C 表示谱形变化，D 表示相对所选参考的偏差，D 越小越接近参考。10–20 kHz 有实际共同数据才显示 Dhigh；20 kHz 以上只显示实际数据。它们描述计算结果，不代表主观音质；测量设备、补偿、佩戴和听感差异请结合备注与试听判断。

## 读取歌单和导出

在内置网页打开已有网易云歌单，点击“读取歌单”，选择已采集的真实录音并手动绑定对应原始行。页面只能读到可见内容时，应用会保留已读歌曲及完整度说明。

在“根据耳机找歌曲”中点击“生成网易云歌单”，勾选歌曲、填写名称，首次使用时点击“连接网易云网页账号”完成网页登录，再点击创建。桌面客户端用于听歌和采集；网页账号用于创建歌单，登录保存在本机。应用会新建歌单并核对歌曲与顺序，结果提供目标链接。未关联网易云 ID 的本地录音可先在资料库绑定歌曲链接。

遇到结果待核对时，先打开网易云查看；可粘贴已创建歌单的链接重新核对，也可以人工确认后开始新的创建，应用不会自动反复新建。本地 JSON 导出仍可使用。

## 本地数据和来源

资料目录：`~/Library/Application Support/Resonance`。其中包含元数据、原始音频和频谱文件，数据留在本机。

拉斐尔原始来源：[HBB AudioTools 文件](https://hbb.squig.link/data/Artipical%20Raphael%20R.txt)。sample 是公开的右声道实测数据，不是本项目自行测量，20 kHz 以上没有补点。

内置偏好参考的原始页面、CSV、点数和文件摘要见 [数据来源](Samples/PersonalReferences/README.md)。两条测量最高约 19.897 kHz，20 kHz 以上没有补点。跨来源/夹具和头戴/入耳式之间的比较需结合佩戴与试听；新模型是相对频谱估计，没有把数字域能量当作耳膜声压，也没有假定 MEMS 标签自动加分。
